// Implements: REQ-p00006-B
// Implements: REQ-d00232-A, REQ-d00232-B
// Partial: _card-stack.js.j2 — Card rendering, open/close

// =====================================================================
// Card Stack — Unified for all node kinds
// =====================================================================

// Comment data cache: nodeId -> { anchor: [thread, ...], ... }
var cardCommentData = {};

function updateTreeCommentFlag(nodeId) {
    // Update has_comments in cached treeData and re-render tree
    var hasComments = cardCommentData[nodeId] &&
        Object.keys(cardCommentData[nodeId]).length > 0;
    if (editState.treeData) {
        editState.treeData.forEach(function(row) {
            if (row.id === nodeId) {
                row.has_comments = hasComments;
            }
        });
        renderNavTree();
    }
}

async function openCard(nodeId) {
    editState.focusNodeId = nodeId;
    // If already open, just focus it
    if (editState.openCards.has(nodeId)) {
        focusCard(nodeId);
        return;
    }

    // Fetch node data via generic endpoint
    const data = await apiFetch('/api/node/' + encodeURIComponent(nodeId));
    if (!data) return;

    // Store in state with kind from response
    editState.openCards.set(nodeId, { data: data, collapsed: false, kind: data.kind });

    // Add to front of order
    editState.openCardOrder = editState.openCardOrder.filter(function(id) { return id !== nodeId; });
    editState.openCardOrder.unshift(nodeId);

    renderCardStack();
    // Prefetch coverage data so buttons are colored immediately
    if (data.kind === 'requirement') { prefetchCoverage(nodeId); }
    // Fetch comment data for margin indicators
    loadCardComments(nodeId);
    focusCard(nodeId);
    updateNavSelection();
    saveState();
}

function closeCard(nodeId) {
    editState.openCards.delete(nodeId);
    editState.openCardOrder = editState.openCardOrder.filter(function(id) { return id !== nodeId; });
    renderCardStack();
    updateNavSelection();
    saveState();
}

function focusCard(nodeId) {
    // Move to front of order
    editState.openCardOrder = editState.openCardOrder.filter(function(id) { return id !== nodeId; });
    editState.openCardOrder.unshift(nodeId);

    // Render first so DOM is rebuilt with correct order, then scroll
    renderCardStack();
    saveState();

    // Re-query after render since renderCardStack() replaces all DOM nodes
    document.querySelectorAll('.req-card').forEach(function(c) { c.classList.remove('focused'); });
    const card = document.getElementById('card-' + nodeId);
    if (card) {
        card.classList.add('focused');
        card.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }
}

function toggleCardCollapse(nodeId) {
    const info = editState.openCards.get(nodeId);
    if (!info) return;
    info.collapsed = !info.collapsed;
    const card = document.getElementById('card-' + nodeId);
    if (card) {
        card.classList.toggle('collapsed', info.collapsed);
    }
    saveState();
}

function closeAllCards() {
    editState.openCards.clear();
    editState.openCardOrder = [];
    renderCardStack();
    updateNavSelection();
    saveState();
}

function renderCardStack() {
    const body = document.getElementById('card-stack-body');
    const empty = document.getElementById('card-stack-empty');
    const countEl = document.getElementById('card-stack-count');

    if (editState.openCardOrder.length === 0) {
        body.innerHTML = '';
        body.appendChild(empty);
        empty.style.display = 'flex';
        countEl.textContent = 'No cards open';
        return;
    }

    empty.style.display = 'none';
    countEl.textContent = editState.openCardOrder.length + ' card' +
        (editState.openCardOrder.length !== 1 ? 's' : '') + ' open';

    let html = '';
    editState.openCardOrder.forEach(function(nodeId) {
        const info = editState.openCards.get(nodeId);
        if (!info) return;
        var kind = info.kind || 'requirement';
        if (kind === 'journey') {
            html += buildJourneyCardHtml(nodeId, info.data, info.collapsed);
        } else if (kind === 'term') {
            html += buildTermCardHtml(nodeId, info.data, info.collapsed);
        } else if (kind === 'requirement') {
            html += buildCardHtml(nodeId, info.data, info.collapsed);
        } else {
            html += buildGenericCardHtml(nodeId, info.data, info.collapsed);
        }
    });

    body.innerHTML = html;

    // Re-add empty element (hidden) for later
    body.appendChild(empty);

    // Per-assertion badge colors are baked into the rendered HTML from the
    // server-computed assertion_coverage_states (REQ-d00258-G), so a DOM
    // rebuild already carries the correct colors — no re-color pass needed.

    // Re-apply edit mode state after DOM rebuild. In edit mode each
    // assertion's body must show raw source (so textContent roundtrips
    // through onAssertionBlur unchanged); in view mode it shows the
    // markdown-rendered HTML produced by simpleMarkdown.
    document.querySelectorAll('.card-assertion-text').forEach(function(el) {
        var raw = el.dataset.original || '';
        if (editState.enabled) {
            el.contentEditable = 'true';
            el.innerHTML = escapeHtml(raw);
        } else {
            el.contentEditable = 'false';
        }
    });

    // Re-apply cached comment indicators after DOM rebuild
    editState.openCardOrder.forEach(function(nodeId) {
        if (cardCommentData[nodeId]) {
            renderMarginIndicators(nodeId, cardCommentData[nodeId]);
        }
    });
}

// =====================================================================
// Lost Comments Card (Implements: REQ-d00234-A)
// =====================================================================

function loadOrphanedComments() {
    apiFetch('/api/comments/orphaned').then(function(data) {
        if (data && data.success && data.threads.length > 0) {
            renderLostCommentsCard(data.threads);
        } else {
            // Remove existing lost comments card if no orphans
            var existing = document.getElementById('lost-comments-card');
            if (existing) existing.remove();
        }
    });
}

function renderLostCommentsCard(threads) {
    var existing = document.getElementById('lost-comments-card');
    if (existing) existing.remove();

    var html = '<div class="req-card lost-comments-card" id="lost-comments-card">';
    html += '<div class="req-card-header lost-comments-header">';
    html += '<span class="lost-comments-icon">&#9888;</span>';
    html += '<span class="lost-comments-title">Lost Comments (' + threads.length + ')</span>';
    html += '<button class="req-card-close" onclick="document.getElementById(\'lost-comments-card\').remove()" title="Dismiss">&times;</button>';
    html += '</div>';
    html += '<div class="req-card-body">';
    threads.forEach(function(t) {
        html += '<div class="lost-comment-item">';
        html += '<div class="lost-comment-origin">Originally on <code>' +
            escapeHtml(t.anchor || t.root.anchor) + '</code> (deleted)</div>';
        html += '<div class="comment-entry">';
        html += '<span class="comment-author">' + escapeHtml(t.root.author) + '</span>';
        html += ' <span class="comment-date">' + escapeHtml(t.root.date) + '</span>';
        html += '<div class="comment-text">' + escapeHtml(t.root.text) + '</div>';
        html += '</div>';
        if (t.replies && t.replies.length > 0) {
            t.replies.forEach(function(r) {
                html += '<div class="comment-entry comment-reply">';
                html += '<span class="comment-author">' + escapeHtml(r.author) + '</span>';
                html += ' <span class="comment-date">' + escapeHtml(r.date) + '</span>';
                html += '<div class="comment-text">' + escapeHtml(r.text) + '</div>';
                html += '</div>';
            });
        }
        html += '<div class="comment-actions edit-only">';
        html += '<button class="comment-resolve-btn" onclick="resolveLostComment(\'' +
            escapeAttr(t.root.id) + '\')">Resolve</button>';
        html += '</div>';
        html += '</div>';
    });
    html += '</div></div>';

    var body = document.getElementById('card-stack-body');
    if (body) {
        body.insertAdjacentHTML('afterbegin', html);
    }
}

async function resolveLostComment(commentId) {
    var data = await apiFetch('/api/comment/resolve', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({comment_id: commentId})
    });
    if (data && data.success) {
        showToast('Comment resolved', 'success');
        loadOrphanedComments();
    }
}

// =====================================================================
// Comment Margin Indicators (REQ-d00232-B)
// =====================================================================

function loadCardComments(nodeId) {
    apiFetch('/api/comments/card?node_id=' + encodeURIComponent(nodeId))
        .then(function(data) {
            if (data && data.success) {
                cardCommentData[nodeId] = data.threads;
                renderMarginIndicators(nodeId, data.threads);
            }
        });
}

function renderMarginIndicators(nodeId, threads) {
    var cardEl = document.getElementById('card-' + nodeId);
    if (!cardEl) return;
    // Check if this card has any comments at all
    var hasAnyComments = threads && Object.keys(threads).length > 0;
    var anchored = cardEl.querySelectorAll('[data-anchor]');
    anchored.forEach(function(el) {
        var anchor = el.getAttribute('data-anchor');
        // Remove previous gutter state
        el.classList.remove('has-comment-gutter', 'comment-gutter-empty');
        var existingGutter = el.querySelector('.comment-gutter');
        if (existingGutter) existingGutter.remove();
        var count = threads[anchor] ? threads[anchor].length : 0;
        if (count > 0) {
            el.classList.add('has-comment-gutter');
            // Absolute-positioned icon in the left gutter area
            var gutter = document.createElement('span');
            gutter.className = 'comment-gutter';
            gutter.title = count + ' comment' + (count > 1 ? 's' : '') + ' — click to expand';
            gutter.innerHTML = '<span class="comment-gutter-bubble">\uD83D\uDDE9</span><span class="comment-gutter-count">' + count + '</span>';
            gutter.onclick = function(e) {
                e.stopPropagation();
                toggleCommentThread(anchor);
            };
            el.style.position = 'relative';
            el.appendChild(gutter);
        } else if (hasAnyComments) {
            // Empty gutter placeholder so all rows align
            el.classList.add('comment-gutter-empty');
        }
    });
}

// Implements: REQ-d00233-A
function toggleCommentThread(anchor) {
    var existing = document.querySelector('.comment-thread-inline[data-thread-anchor="' + anchor + '"]');
    if (existing) {
        existing.remove();
        return;
    }
    var el = document.querySelector('[data-anchor="' + anchor + '"]');
    if (!el) return;
    var card = el.closest('.req-card');
    if (!card) return;
    var nodeId = (card.id || '').replace(/^card-/, '');
    if (!nodeId) return;
    var threads = cardCommentData[nodeId];
    if (!threads || !threads[anchor]) return;
    var container = document.createElement('div');
    container.className = 'comment-thread-inline';
    container.setAttribute('data-thread-anchor', anchor);
    threads[anchor].forEach(function(t) {
        container.appendChild(buildThreadHtml(t, anchor, nodeId));
    });
    el.after(container);
}

function buildThreadHtml(thread, anchor, nodeId) {
    var div = document.createElement('div');
    div.className = 'comment-thread-item';
    div.setAttribute('data-comment-id', thread.root.id);
    // Root comment
    var html = '<div class="comment-entry">';
    html += '<span class="comment-author">' + escapeHtml(thread.root.author) + '</span>';
    html += ' <span class="comment-date">' + escapeHtml(thread.root.date) + '</span>';
    html += '<div class="comment-text">' + escapeHtml(thread.root.text) + '</div>';
    html += '</div>';
    // Replies
    if (thread.replies && thread.replies.length > 0) {
        thread.replies.forEach(function(r) {
            html += '<div class="comment-entry comment-reply">';
            html += '<span class="comment-author">' + escapeHtml(r.author) + '</span>';
            html += ' <span class="comment-date">' + escapeHtml(r.date) + '</span>';
            html += '<div class="comment-text">' + escapeHtml(r.text) + '</div>';
            html += '</div>';
        });
    }
    // Edit-mode-only controls: Resolve and Reply
    html += '<div class="comment-actions edit-only">';
    html += '<button class="comment-resolve-btn" onclick="resolveComment(\'' +
        escapeAttr(thread.root.id) + '\', \'' + escapeAttr(anchor) + '\', \'' +
        escapeAttr(nodeId) + '\')">Resolve</button>';
    html += '<button class="comment-reply-btn" onclick="showReplyInput(this, \'' +
        escapeAttr(thread.root.id) + '\', \'' + escapeAttr(anchor) + '\', \'' +
        escapeAttr(nodeId) + '\')">Reply</button>';
    html += '</div>';
    div.innerHTML = html;
    return div;
}

async function resolveComment(commentId, anchor, nodeId) {
    var data = await apiFetch('/api/comment/resolve', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({comment_id: commentId})
    });
    if (data && data.success) {
        // Remove thread item from DOM; if it was the last one, remove the container too
        var threadEl = document.querySelector('.comment-thread-item[data-comment-id="' + commentId + '"]');
        if (threadEl) {
            var container = threadEl.closest('.comment-thread-inline');
            threadEl.remove();
            if (container && container.querySelectorAll('.comment-thread-item').length === 0) {
                container.remove();
            }
        }
        // Remove from cache; clean up empty anchor keys
        if (cardCommentData[nodeId] && cardCommentData[nodeId][anchor]) {
            cardCommentData[nodeId][anchor] = cardCommentData[nodeId][anchor].filter(
                function(t) { return t.root.id !== commentId; }
            );
            if (cardCommentData[nodeId][anchor].length === 0) {
                delete cardCommentData[nodeId][anchor];
            }
        }
        // Refresh margin indicators and tree
        renderMarginIndicators(nodeId, cardCommentData[nodeId] || {});
        updateTreeCommentFlag(nodeId);
        showToast('Comment resolved', 'success');
    }
}

function showReplyInput(btn, parentId, anchor, nodeId) {
    // Remove any existing reply input
    var existing = btn.closest('.comment-thread-item').querySelector('.comment-reply-input');
    if (existing) { existing.remove(); return; }
    var inputDiv = document.createElement('div');
    inputDiv.className = 'comment-reply-input';
    inputDiv.innerHTML = '<textarea class="comment-input-text" placeholder="Write a reply..." rows="2"></textarea>' +
        '<div class="comment-input-actions">' +
        '<button class="comment-submit-btn" onclick="submitReply(this, \'' +
        escapeAttr(parentId) + '\', \'' + escapeAttr(anchor) + '\', \'' +
        escapeAttr(nodeId) + '\')">Reply</button>' +
        '<button class="comment-cancel-btn" onclick="this.closest(\'.comment-reply-input\').remove()">Cancel</button>' +
        '</div>';
    btn.closest('.comment-thread-item').appendChild(inputDiv);
    inputDiv.querySelector('textarea').focus();
}

async function submitReply(btn, parentId, anchor, nodeId) {
    var textarea = btn.closest('.comment-reply-input').querySelector('textarea');
    var text = textarea.value.trim();
    if (!text) return;
    var data = await apiFetch('/api/comment/reply', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({parent_id: parentId, text: text})
    });
    if (data && data.success) {
        // Append reply to DOM
        var threadItem = btn.closest('.comment-thread-item');
        var replyDiv = document.createElement('div');
        replyDiv.className = 'comment-entry comment-reply';
        replyDiv.innerHTML = '<span class="comment-author">' + escapeHtml(data.comment.author) + '</span>' +
            ' <span class="comment-date">' + escapeHtml(data.comment.date) + '</span>' +
            '<div class="comment-text">' + escapeHtml(data.comment.text) + '</div>';
        var actions = threadItem.querySelector('.comment-actions');
        threadItem.insertBefore(replyDiv, actions);
        // Remove input
        btn.closest('.comment-reply-input').remove();
        showToast('Reply added', 'success');
    }
}

// Implements: REQ-d00233-B
function showCommentInput(anchor, afterElement) {
    // Remove any existing comment input and highlight
    var existing = document.querySelector('.comment-new-input');
    if (existing) existing.remove();
    var prevTarget = document.querySelector('.comment-target');
    if (prevTarget) prevTarget.classList.remove('comment-target');

    // Remember clicked element's position before DOM changes
    var scrollContainer = document.getElementById('card-stack-body');
    var elTopBefore = afterElement.getBoundingClientRect().top;

    // Expand any existing comment threads on this anchor so user sees context
    var card = afterElement.closest('.req-card');
    if (card) {
        var nodeId = (card.id || '').replace(/^card-/, '');
        var threads = cardCommentData[nodeId];
        if (threads && threads[anchor]) {
            var existingThread = document.querySelector('.comment-thread-inline[data-thread-anchor="' + anchor + '"]');
            if (!existingThread) {
                toggleCommentThread(anchor);
            }
        }
    }

    // Highlight the target element
    afterElement.classList.add('comment-target');
    var inputDiv = document.createElement('div');
    inputDiv.className = 'comment-new-input';
    inputDiv.innerHTML = '<textarea class="comment-input-text" placeholder="Add a comment..." rows="3"></textarea>' +
        '<div class="comment-input-actions">' +
        '<button class="comment-submit-btn" onclick="submitNewComment(this, \'' +
        escapeAttr(anchor) + '\')">Submit</button>' +
        '<button class="comment-cancel-btn" onclick="cancelCommentInput()">Cancel</button>' +
        '</div>';
    afterElement.after(inputDiv);

    // Restore scroll so clicked element stays in the same visual position
    if (scrollContainer) {
        var elTopAfter = afterElement.getBoundingClientRect().top;
        var drift = elTopAfter - elTopBefore;
        if (Math.abs(drift) > 1) {
            scrollContainer.scrollTop += drift;
        }
    }

    inputDiv.querySelector('textarea').focus();
    // Esc to cancel
    inputDiv.querySelector('textarea').addEventListener('keydown', function(e) {
        if (e.key === 'Escape') { cancelCommentInput(); }
    });
}

function cancelCommentInput() {
    var input = document.querySelector('.comment-new-input');
    if (input) input.remove();
    var target = document.querySelector('.comment-target');
    if (target) target.classList.remove('comment-target');
}

async function submitNewComment(btn, anchor) {
    var textarea = btn.closest('.comment-new-input').querySelector('textarea');
    var text = textarea.value.trim();
    if (!text) return;
    var data = await apiFetch('/api/comment/add', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({anchor: anchor, text: text})
    });
    if (data && data.success) {
        // Remove input and highlight
        btn.closest('.comment-new-input').remove();
        var target = document.querySelector('.comment-target');
        if (target) target.classList.remove('comment-target');
        // Find the card and reload comments, then show the thread
        var el = document.querySelector('[data-anchor="' + anchor + '"]');
        if (el) {
            var card = el.closest('.req-card');
            if (card) {
                var nodeId = (card.id || '').replace(/^card-/, '');
                // Reload comment data, then refresh indicators and show thread
                var refreshed = await apiFetch('/api/comments/card?node_id=' + encodeURIComponent(nodeId));
                if (refreshed && refreshed.success) {
                    cardCommentData[nodeId] = refreshed.threads;
                    renderMarginIndicators(nodeId, refreshed.threads);
                    // Auto-expand the thread on the anchor we just commented on
                    // Remove any existing inline thread first
                    var existing = document.querySelector('.comment-thread-inline[data-thread-anchor="' + anchor + '"]');
                    if (existing) existing.remove();
                    toggleCommentThread(anchor);
                }
                updateTreeCommentFlag(nodeId);
            }
        }
        showToast('Comment added', 'success');
    }
}

// Per-assertion coverage STANDING -> CSS class, resolved from the theme catalog
// ([coverage_standing.*]) — NOT hard-coded here (REQ-d00258-G). The server emits
// a semantic standing token (full/partial/failing/missing); this map is rendered
// from the catalog so the standing->color association stays configurable (e.g.
// remappable for color-impaired users), exactly like the severity colors. The
// classes are the SAME val-* classes the requirement header badges use, so a
// per-assertion badge reads identically to the requirement badge it projects from.
var COVERAGE_STANDING_CLASS = {
{%- if catalog is defined %}
{%- for e in catalog.by_category('coverage_standing') %}
    '{{ e.key.split('.')[-1] }}': '{{ e.css_class }}'{{ "," if not loop.last }}
{%- endfor %}
{%- endif %}
};
function assertionStateColorClass(standing) {
    return COVERAGE_STANDING_CLASS[standing] || '';
}

function buildAssertionHtml(nodeId, a, data) {
    var assertionId = a.id || '';
    var label = a.label || '';
    var text = a.text || '';
    var h = '';
    h += '<div class="card-assertion-wrapper" data-assertion-id="' + assertionId + '" data-anchor="' + nodeId + '#' + label + '">';
    h += '<div class="card-assertion">';
    h += '<span class="card-assertion-label">' + label + '</span>';
    // Assertion text — rendered through simpleMarkdown in view mode so
    // pipe tables and inline bold render. In edit mode the span swaps to
    // raw escaped text via _activateEditMode/_deactivateEditMode so the
    // user types against the markdown source (textContent roundtrips
    // back to the same source on blur).
    var initialBody = editState.enabled
        ? escapeHtml(text)
        : simpleMarkdown(text, true);
    h += '<span class="card-assertion-text editable-field" ' +
        'contenteditable="false" data-assertion-id="' + assertionId + '" ' +
        'data-field="assertion" data-original="' + escapeAttr(text) + '" ' +
        'onblur="onAssertionBlur(this)" onkeydown="onEditableKeydown(event)">' +
        initialBody + '</span>';
    // 6 dimension badges — shown when a direct assertion-level link exists OR
    // the assertion has a non-missing coverage state (REQ-d00258-G)
    h += '<span class="assertion-badges-area">';
    var badgeMode = editState.assertionBadgeMode || 'abbrev';
    var aLinks = data.assertion_links ? data.assertion_links[label] : null;
    // Per-assertion coverage states, server-computed from the SAME rollup metrics
    // as the requirement header badges (REQ-d00258-G). Color is applied on render
    // (no lazy prefetch): state -> the val-* class the requirement badge uses.
    var aStates = data.assertion_coverage_states ? data.assertion_coverage_states[label] : null;
    var aCaveats = data.assertion_coverage_caveats ? data.assertion_coverage_caveats[label] : null;
    var dimDefs = [
        {cls: 'assertion-implemented-btn', full: 'Implemented', abbr: 'IMP', dot: 'I', toggle: 'toggleAssertionCode', tip: 'Code implementation', linkKey: 'implemented', stateKey: 'implemented', edgeKind: 'implements'},
        {cls: 'assertion-refined-btn', full: 'Refined', abbr: 'REF', dot: 'R', toggle: 'toggleAssertionRefines', tip: 'Refines references', linkKey: 'refined'},
        {cls: 'assertion-tested-btn', full: 'Tested', abbr: 'TST', dot: 'T', toggle: 'toggleAssertionTests', tip: 'Test coverage', linkKey: 'tested', stateKey: 'tested'},
        {cls: 'assertion-verified-btn', full: 'Passing', abbr: 'VER', dot: 'V', toggle: 'toggleAssertionResults', tip: 'Test results', linkKey: 'tested', stateKey: 'verified'},
        {cls: 'assertion-validated-btn', full: 'UAT Covered', abbr: 'VAL', dot: 'U', toggle: 'toggleAssertionUat', tip: 'Journey coverage', linkKey: 'validated', stateKey: 'uat_coverage'},
        {cls: 'assertion-accepted-btn', full: 'UAT Passed', abbr: 'ACC', dot: 'A', toggle: 'toggleAssertionUatResults', tip: 'Journey results', linkKey: 'validated', stateKey: 'uat_verified'}
    ];
    var anyBadge = false;
    dimDefs.forEach(function(dim) {
        var st = (dim.stateKey && aStates) ? aStates[dim.stateKey] : null;
        var hasState = st === 'full' || st === 'partial' || st === 'failing';
        // Render when there's a direct assertion-level link OR a non-missing
        // coverage state (whole-req evidence now shows as a pill, not "no
        // coverage"). State-less pills (REF: linkKey but no stateKey) keep the
        // strict link-gate -- they have no state to fall back on.
        if (aLinks && !aLinks[dim.linkKey] && !(dim.stateKey && hasState)) return;
        anyBadge = true;
        var btnLabel = badgeMode === 'full' ? dim.full : (badgeMode === 'dots' ? dim.dot : dim.abbr);
        // Non-transitive ~ caveat: whole-requirement evidence involved.
        if (dim.stateKey && aCaveats && aCaveats[dim.stateKey] && hasState) {
            btnLabel += ' ~';
        }
        var btnStyle = badgeMode === 'dots' ? ' style="min-width:0;padding:2px 4px;font-size:0.55rem;"' : '';
        var extraArg = dim.edgeKind ? ", '" + dim.edgeKind + "'" : '';
        var stateCls = '';
        if (dim.stateKey && aStates && aStates[dim.stateKey]) {
            stateCls = assertionStateColorClass(aStates[dim.stateKey]);
        }
        // Data-driven tooltip: coverage standing + direct-link count, with
        // the "~" legend only when the caveat marker is actually shown.
        var tipParts = [dim.tip];
        if (dim.stateKey && aStates && aStates[dim.stateKey]) {
            tipParts.push('standing: ' + aStates[dim.stateKey]);
        }
        var dimLinks = aLinks ? aLinks[dim.linkKey] : null;
        if (dimLinks && dimLinks.length) {
            tipParts.push(dimLinks.length + ' direct link(s) — click to view');
        } else {
            tipParts.push('click to view');
        }
        var pillTip = tipParts.join(' — ');
        if (dim.stateKey && aCaveats && aCaveats[dim.stateKey] && hasState) {
            pillTip += ' (~ = some coverage comes from whole-requirement references)';
        }
        h += '<button class="' + dim.cls + (stateCls ? ' ' + stateCls : '') + '" ' +
            'data-req-id="' + nodeId + '" data-label="' + escapeAttr(label) + '" ' +
            'title="' + escapeAttr(pillTip) + '" ' +
            'onclick="event.stopPropagation(); ' + dim.toggle + '(this, \'' + nodeId + '\', \'' + escapeAttr(label) + '\'' + extraArg + ')"' +
            btnStyle + '>' +
            btnLabel + '</button>';
    });
    if (!anyBadge) {
        // INSTANCE assertions inherit coverage from the template original
        // (see REQ-p00014-K). Show that instead of the bare "no coverage"
        // hint so the viewer reflects the satisfier-rollup invariant.
        var aTplId = a.template_id || '';
        var aTplRepo = a.template_repo || '';
        var aInherited = (a.inherited_coverage != null) ? a.inherited_coverage : null;
        if (aTplId || aTplRepo || aInherited != null) {
            var inheritedText = 'inherits from template';
            if (aTplId) inheritedText = 'inherits from ' + aTplId;
            var inheritedTip = aTplRepo ? ('Template defined in ' + aTplRepo) : 'Coverage inherited from template';
            if (aInherited != null) inheritedTip += ' — ' + aInherited + ' direct link(s) on template';
            h += '<span class="assertion-inherited-coverage" title="' + escapeAttr(inheritedTip) + '" ' +
                'style="font-size:0.6rem;color:var(--secondary-text);font-style:italic;">' +
                escapeHtml(inheritedText) + '</span>';
        } else {
            h += '<span class="assertion-no-coverage" title="No coverage on either footing" style="font-size:0.6rem;color:var(--secondary-text);opacity:0.5;">no coverage</span>';
        }
    }
    h += '</span>'; // close .assertion-badges-area
    h += '<button class="assertion-delete-btn edit-only" ' +
        'title="Deprecate assertion" ' +
        'onclick="event.stopPropagation(); onDeleteAssertion(\'' + nodeId + '\', \'' + escapeAttr(assertionId) + '\')">' +
        '&#8856;</button>';
    h += '</div>';
    h += '<div class="assertion-code-panel" id="assertion-code-' + nodeId + '-' + label + '" style="display:none;"></div>';
    h += '<div class="assertion-refines-panel" id="assertion-refines-' + nodeId + '-' + label + '" style="display:none;"></div>';
    h += '<div class="assertion-tests-panel" id="assertion-tests-' + nodeId + '-' + label + '" style="display:none;"></div>';
    h += '<div class="assertion-results-panel" id="assertion-results-' + nodeId + '-' + label + '" style="display:none;"></div>';
    h += '<div class="assertion-uat-panel" id="assertion-uat-' + nodeId + '-' + label + '" style="display:none;"></div>';
    h += '<div class="assertion-uat-results-panel" id="assertion-uat-results-' + nodeId + '-' + label + '" style="display:none;"></div>';
    h += '</div>';
    return h;
}

function buildRemainderHtml(nodeId, rem, idx) {
    var h = '';
    var remId = rem.id || '';
    var isPreamble = (rem.heading === 'preamble');
    var isDefBlock = (rem.content_type === 'definition_block');
    h += '<div class="card-body-section-wrapper" data-remainder-id="' + escapeAttr(remId) + '" data-anchor="' + nodeId + '#section:' + escapeAttr(rem.heading || '') + '">';
    h += '<div class="card-body-section-heading">';
    if (!isDefBlock && !isPreamble) {
        h += '<span class="remainder-heading-view">' + escapeHtml(rem.heading) + '</span>';
        h += '<input type="text" class="edit-only remainder-heading-edit" value="' + escapeAttr(rem.heading) + '"' +
            ' data-node-id="' + escapeAttr(remId) + '" data-original="' + escapeAttr(rem.heading) + '"' +
            ' onblur="onRemainderHeadingBlur(this)" style="font-weight:600;flex:1;">';
    } else {
        h += '<span>' + escapeHtml(rem.heading) + '</span>';
    }
    h += '<span class="card-body-section-toggle" id="body-section-toggle-' + nodeId + '-' + idx + '" ' +
        'onclick="toggleBodySection(\'' + nodeId + '\', ' + idx + ')">[-]</span>';
    if (!isDefBlock) {
        h += '<button class="edit-only remainder-delete-btn" ' +
            'onclick="event.stopPropagation(); onRemainderDelete(\'' + escapeAttr(remId) + '\')" ' +
            'title="Delete section">&times;</button>';
    }
    h += '</div>';
    h += '<div class="card-body-section-content remainder-text-view" id="body-section-content-' + nodeId + '-' + idx + '">' +
        simpleMarkdown(rem.text || '', true) + '</div>';
    if (!isDefBlock) {
        h += '<textarea class="edit-only remainder-text-edit" data-node-id="' + escapeAttr(remId) + '"' +
            ' data-original="' + escapeAttr(rem.text || '') + '"' +
            ' onblur="onRemainderTextBlur(this)"' +
            ' style="width:100%;min-height:120px;font-size:0.8rem;border:1px solid var(--border-color);border-radius:4px;padding:6px;resize:vertical;background:var(--card-bg);color:var(--input-text);">' +
            escapeHtml(rem.text || '') + '</textarea>';
    }
    h += '</div>';
    return h;
}

function buildCardHtml(nodeId, data, collapsed) {
    const collapsedClass = collapsed ? ' collapsed' : '';
    const focusedClass = editState.openCardOrder[0] === nodeId ? ' focused' : '';
    const shortId = nodeId;
    const title = escapeHtml(data.title || '');
    var props = data.properties || {};
    const level = (props.level || '').toUpperCase();
    const status = (props.status || '').toLowerCase();
    const statusDisplay = status.charAt(0).toUpperCase() + status.slice(1);
    const hash = props.hash || '';
    const sourcePath = (data.source && data.source.path) || '';
    const sourceLine = (data.source && data.source.line) || 0;

    let html = '<div class="req-card' + collapsedClass + focusedClass + '" id="card-' + nodeId + '">';

    // Header — with inline title editing
    html += '<div class="req-card-header" onclick="toggleCardCollapse(\'' + nodeId + '\')">';
    html += '<span class="req-card-expand">&#9660;</span>';
    html += '<span class="req-card-id">' + shortId + '</span>';
    // Explicit **Template** marker (Stereotype.TEMPLATE) — authors/reviewers
    // must see template-ness at a glance; instances show provenance instead.
    if ((props.stereotype || '').toLowerCase() === 'template') {
        html += '<span class="template-badge" title="Template requirement: Satisfies: references clone its assertions as instances">TEMPLATE</span>';
    }
    html += '<span class="req-card-title-display view-only">' + title + '</span>';
    html += '<input type="text" class="edit-only req-card-title-edit" value="' + escapeAttr(data.title || '') + '"' +
        ' onclick="event.stopPropagation()" onblur="onTitleBlur(this)" data-node-id="' + nodeId + '"' +
        ' data-original="' + escapeAttr(data.title || '') + '"' +
        ' style="font-size:0.9rem;font-weight:600;border:none;border-bottom:1px dashed var(--border-color);background:transparent;color:var(--input-text);flex:1;outline:none;padding:1px 4px;">';
    html += '<button class="req-card-delete edit-only" ' +
        'onclick="event.stopPropagation(); onDeleteRequirement(\'' + nodeId + '\')" ' +
        'title="Delete requirement">&#x1F5D1;</button>';
    html += '<button class="req-card-close" onclick="event.stopPropagation(); closeCard(\'' + nodeId + '\')" title="Close card">&times;</button>';
    html += '</div>';

    // Body
    html += '<div class="req-card-body">';

    // Coverage dimension badges (above metadata)
    // Only shown when data exists; always clickable to expand links + flash assertions
    if (data.coverage_dimensions) {
        var rLinks = data.req_level_links || {};
        var aLinksAll = data.assertion_links || {};
        html += '<div class="card-section card-coverage-badges" style="padding:4px 12px 0;display:flex;gap:6px;flex-wrap:wrap;">';
        var headerDims = [
            // No standalone REFINED badge: refinement is implementation
            // provenance ("implemented via refinement, not directly"), folded
            // into the IMPLEMENTED badge's drill-down below — mirroring the
            // per-assertion IMP pill (REQ-d00069-L).
            {key: 'implemented', linkKey: 'implemented', assertLinkKey: 'implemented', label: 'IMPLEMENTED'},
            {key: 'tested', linkKey: 'tested', assertLinkKey: 'tested', label: 'TESTED'},
            {key: 'verified', linkKey: 'verified', assertLinkKey: 'tested', label: 'PASSING'},
            {key: 'uat_coverage', linkKey: 'validated', assertLinkKey: 'validated', label: 'UAT COVERED', gateOnEvidence: true},
            {key: 'uat_verified', linkKey: 'accepted', assertLinkKey: 'validated', label: 'UAT PASSED', gateOnEvidence: true}
        ];
        headerDims.forEach(function(d) {
            var dim = d.key ? data.coverage_dimensions[d.key] : null;
            var hasReqLinks = d.linkKey && rLinks[d.linkKey] && rLinks[d.linkKey].length > 0;
            // Check if any assertion has direct links for this dimension
            var hasAssertionLinks = false;
            for (var lbl in aLinksAll) {
                if (aLinksAll[lbl][d.assertLinkKey]) { hasAssertionLinks = true; break; }
            }
            // UAT dims (gateOnEvidence) render with actual journey Validates
            // evidence — a journey-less requirement shows no UAT badge (its colored
            // "missing" tier is not a real gap). Exception: when the requirement's
            // level expects_validation, a journey-less req DOES show the badge —
            // in RED (its "missing" tier resolves to error severity server-side), a
            // real gap. Code dims still show on 0-coverage.
            var expectsValidation = dim && dim.expects_validation;
            var hasData = d.gateOnEvidence
                ? (hasReqLinks || hasAssertionLinks || expectsValidation)
                : ((dim && dim.color) || hasReqLinks || hasAssertionLinks);
            if (!hasData) return; // hide badge if no data
            var colorClass = dim && dim.color ? ' active val-' + dim.color : '';
            var tipParts = [];
            if (dim && dim.status_tip) tipParts.push(dim.status_tip);
            if (hasReqLinks) tipParts.push(rLinks[d.linkKey].length + ' REQ-level link(s)');
            if (hasAssertionLinks) tipParts.push('Click to highlight assertions');
            var tip = tipParts.length > 0 ? tipParts.join(' \u2014 ') : d.label;
            // Per-dimension provenance caveat (REQ-d00069-L): a "~" beside the
            // badge glyph flags coverage whose evidence is not fully direct
            // (indirect > direct). The enriched status_tip (above) already spells
            // out the "Nn% direct, Mm% indirect" provenance on hover.
            var marker = dim && dim.marker ? dim.marker : '';
            var markerGlyph = marker
                ? '<sup class="dim-caveat" style="font-weight:700;opacity:0.85;">' + escapeHtml(marker) + '</sup>'
                : '';
            html += '<span class="status-badge' + colorClass + ' card-dim-badge" ' +
                'title="' + escapeHtml(tip) + '" style="font-size:0.6rem;padding:1px 5px;" ' +
                'onclick="event.stopPropagation(); toggleReqLevelLinks(this, \'' + nodeId + '\', \'' + d.linkKey + '\', \'' + d.assertLinkKey + '\')">' +
                d.label + markerGlyph + '</span>';
        });
        html += '</div>';
        // Expandable link panels (one per header dimension, hidden by default)
        ['implemented', 'tested', 'verified', 'validated', 'accepted'].forEach(function(dimKey) {
            html += '<div class="req-level-links-panel" id="req-links-' + nodeId + '-' + dimKey + '" style="display:none;margin:0 12px;padding:6px 10px;background:var(--assertion-code-panel-bg);border-radius:4px;border-left:3px solid var(--impl-yes-border);font-size:0.78rem;"></div>';
        });
    }

    // Metadata section — source file right-justified on label line
    var reqSourceLink = '';
    if (sourcePath) {
        reqSourceLink = '<span style="margin-left:auto;display:flex;align-items:baseline;gap:4px;text-transform:none;">' +
            '<a href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeJsInAttr(sourcePath) + '\', ' + sourceLine + ', \'' + nodeId + '\')" ' +
            'style="color:#0d6efd;cursor:pointer;font-size:0.7rem;text-decoration:none;">' +
            escapeHtml(sourcePath.split('/').pop()) + ':' + sourceLine + '</a>' +
            '<button class="card-move-file-btn edit-only" ' +
            'onclick="event.preventDefault(); onMoveToFile(\'' + nodeId + '\', \'' + escapeJsInAttr(sourcePath) + '\')" ' +
            'title="Move to different file" style="font-size:0.65rem;">Move</button>' +
            '</span>';
    }
    html += '<div class="card-section" data-anchor="' + nodeId + '"><div class="card-section-label" style="display:flex;align-items:baseline;">Metadata' + reqSourceLink + '</div>';
    html += '<div class="card-meta">';

    // Level
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Level:</span>';
    html += '<span class="card-meta-value">' + level + '</span>';
    html += '</div>';

    // Status (badge or select)
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Status:</span>';
    html += '<span class="status-badge-wrapper"><span class="status-badge ' + status + '">' + statusDisplay + '</span></span>';
    html += '<span class="status-select-wrapper edit-only">';
    html += '<select class="status-select" data-node-id="' + nodeId + '" data-original="' + status + '" onchange="onStatusChange(this)">';
    ['Active', 'Draft', 'Deprecated', 'Proposed'].forEach(function(s) {
        const sel = s.toLowerCase() === status ? ' selected' : '';
        html += '<option value="' + s + '"' + sel + '>' + s + '</option>';
    });
    html += '</select></span>';
    html += '</div>';

    // Template marker toggle (edit mode only; view mode shows the header
    // badge). INSTANCE nodes are read-only synthetic content — no toggle.
    if ((props.stereotype || '').toLowerCase() !== 'instance') {
        html += '<div class="card-meta-item edit-only template-toggle-item">';
        html += '<span class="card-meta-key">Template:</span>';
        html += '<input type="checkbox" class="template-toggle" data-node-id="' + nodeId + '"' +
            ((props.stereotype || '').toLowerCase() === 'template' ? ' checked' : '') +
            ' onchange="onTemplateChange(this)" title="Mark this requirement as a Template (Satisfies: target)">';
        html += '</div>';
    }

    // Hash
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Hash:</span>';
    html += '<span class="card-meta-value" style="font-family:monospace;font-size:0.75rem;">' + hash + '</span>';
    html += '</div>';

    // Combined coverage rollup (satisfier REQs only): own concrete-assertion
    // coverage merged with inherited template-assertion coverage. Surfaced
    // by _serialize_node_generic when the REQ has outbound SATISFIES edges
    // and the rollup total is non-zero. See CUR-1353 Phase 11.
    if (props.satisfier_rollup && props.satisfier_rollup.total > 0) {
        var sr = props.satisfier_rollup;
        var pct = Math.round((sr.covered_fraction || 0) * 100);
        html += '<div class="card-meta-item satisfier-rollup" ' +
            'title="Combined own + inherited assertion coverage for this satisfier REQ">';
        html += '<span class="card-meta-key">Combined coverage:</span>';
        html += '<span class="card-meta-value">' +
            sr.covered + '/' + sr.total + ' (' + pct + '%)' +
            ' <span style="font-size:0.7rem;color:var(--secondary-text);">' +
            '— includes inherited from satisfied templates</span></span>';
        html += '</div>';
    }

    // Integrated coverage rollup (consumer REQs declaring `Integrates:` only):
    // implemented/passing coverage inherited from the external associate
    // (library) node this requirement integrates, across the INTEGRATES edge.
    // Surfaced by _serialize_node_generic when the REQ has integrations.
    // See CUR-1419 / REQ-d00252-D. Fields differ from satisfier_rollup:
    // implemented_covered/total + verified_covered/total (no covered_fraction).
    // NOTE (REQ-d00258-B): despite the `verified_*` field names (kept for
    // wire compatibility), the value is the tested_and_passing() union
    // (result-verified OR line-coverage-credited), so it is labeled
    // "passing" here rather than "verified". `has_failures` flags a red
    // library suite whose covered count can still read full via the union's
    // per-assertion max() -- render a FAILING badge (same validation-fail
    // styling as the UAT verdict badge) so it is never displayed clean.
    if (props.integrates_rollup && props.integrates_rollup.implemented_total > 0) {
        var ir = props.integrates_rollup;
        var ipct = Math.round((ir.implemented_covered / ir.implemented_total) * 100);
        var vpct = ir.verified_total ? Math.round((ir.verified_covered / ir.verified_total) * 100) : 0;
        var irFail = ir.has_failures ?
            ' <span class="validation-fail active" style="font-size:0.65rem;padding:1px 6px;border-radius:3px;display:inline-block;"' +
            ' title="A verifying test result in the integrated library failed">FAILING</span>' : '';
        html += '<div class="card-meta-item integrates-rollup" ' +
            'title="Coverage inherited from the external associate library this requirement integrates">';
        html += '<span class="card-meta-key">Integrated coverage:</span>';
        html += '<span class="card-meta-value">implemented ' + ir.implemented_covered + '/' + ir.implemented_total +
            ' (' + ipct + '%), passing ' + ir.verified_covered + '/' + ir.verified_total + ' (' + vpct + '%)' + irFail +
            ' <span style="font-size:0.7rem;color:var(--secondary-text);">— inherited from an external associate</span></span>';
        html += '</div>';
    }

    // Template provenance (cross-repo INSTANCE): show where the template is
    // defined and link back to the template original. See REQ-p00014-K.
    if ((props.stereotype || '').toLowerCase() === 'instance' && (props.template_id || props.template_repo)) {
        var tplRepo = props.template_repo || '';
        var tplId = props.template_id || '';
        html += '<div class="card-meta-item template-provenance" title="This is a cross-repo instance of a template requirement">';
        html += '<span class="card-meta-key">Template:</span>';
        html += '<span class="card-meta-value">';
        if (tplId) {
            html += '<a class="template-origin-link" href="#" ' +
                'onclick="event.preventDefault(); openCard(\'' + escapeAttr(tplId) + '\')">' +
                escapeHtml(tplId) + '</a>';
        }
        if (tplRepo) {
            html += ' <span class="template-repo-badge" style="font-size:0.7rem;color:var(--secondary-text);">(defined in ' +
                escapeHtml(tplRepo) + ')</span>';
        }
        html += '</span>';
        html += '</div>';
    }

    html += '</div></div>';

    // Parents section (relationship editor in edit mode). Collapsible like
    // Incoming Links: one toggle per edge kind, panels closed by default.
    html += '<div class="card-section"><div class="card-section-label">Outgoing Links</div>';
    html += '<div class="card-parents">';
    if (data.parents && data.parents.length > 0) {
        var parentGroups = {};
        var parentKindOrder = [];
        data.parents.forEach(function(p) {
            var k = (p.edge_kind || 'implements').toLowerCase();
            if (!parentGroups[k]) { parentGroups[k] = []; parentKindOrder.push(k); }
            parentGroups[k].push(p);
        });
        parentKindOrder.forEach(function(kind, gi) {
            var panelId = 'outgoing-' + nodeId + '-' + gi;
            var kindDisplay = kind.charAt(0).toUpperCase() + kind.slice(1);
            html += '<button class="incoming-link-toggle outgoing-link-toggle" ' +
                'onclick="event.stopPropagation(); toggleIncomingLinks(\'' + panelId + '\')">' +
                escapeHtml(kindDisplay) + ' (' + parentGroups[kind].length + ')</button>';
            html += '<div class="req-level-links-panel outgoing-link-panel" id="' + panelId + '" style="display:none;">';
            parentGroups[kind].forEach(function(p) {
                const edgeKind = (p.edge_kind || 'implements').toLowerCase();
                const edgeKindDisplay = edgeKind.charAt(0).toUpperCase() + edgeKind.slice(1);
                html += '<div class="card-parent-row" data-anchor="' + nodeId + '#edge:' + p.id + '">';
                // Edit-only kind toggle (view mode reads the kind off the group header)
                html += '<button class="card-parent-kind-toggle edit-only" ' +
                    'data-source="' + nodeId + '" data-target="' + p.id + '" data-kind="' + edgeKind + '" ' +
                    'onclick="event.preventDefault(); onToggleEdgeKind(this)" ' +
                    'title="Click to cycle edge kind">' + edgeKindDisplay + '</button>';
                var pTargetsJson = (p.assertion_targets && p.assertion_targets.length > 0)
                    ? escapeAttr(JSON.stringify(p.assertion_targets)) : '';
                html += '<a class="card-parent-link" href="#" onclick="event.preventDefault(); openCardAndFlash(\'' + p.id + '\', ' +
                    (pTargetsJson ? 'JSON.parse(this.dataset.targets)' : '[]') + ')"' +
                    (pTargetsJson ? ' data-targets="' + pTargetsJson + '"' : '') + '>';
                html += (p.ref_id || p.id) + ' - ' + escapeHtml(p.title || '');
                if (p.assertion_targets && p.assertion_targets.length > 0) {
                    html += ' <span class="card-parent-targets">[' + p.assertion_targets.join(', ') + ']</span>';
                }
                html += '</a>';
                // Assertion targets picker button (edit mode)
                html += '<button class="card-parent-targets-btn edit-only" ' +
                    'onclick="event.preventDefault(); editEdgeTargets(\'' + nodeId + '\', \'' + p.id + '\', this)" ' +
                    'title="Edit assertion targets">&#9998;</button>';
                html += '<button class="card-parent-delete edit-only" ' +
                    'onclick="event.preventDefault(); onDeleteEdge(\'' + nodeId + '\', \'' + p.id + '\')" ' +
                    'title="Remove relationship">&times;</button>';
                html += '</div>';
            });
            html += '</div>';
        });
    } else {
        html += '<div class="card-parents-empty">No parent relationships</div>';
    }
    html += '</div>';
    // Add relationship button (edit mode only)
    html += '<button class="card-add-rel-btn edit-only" ' +
        'onclick="onAddRelationship(\'' + nodeId + '\', this)">+ Add Relationship</button>';
    html += '</div>';

    // Incoming Links section — reverse traceability (things pointing AT this req).
    // One collapsible toggle per present kind; each link shows a state badge +
    // an informative hover tooltip. (REQ-p00006-A)
    var incoming = data.incoming_links || [];
    if (incoming.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Incoming Links</div>';
        html += '<div class="card-incoming-links">';
        incoming.forEach(function(section, si) {
            var panelId = 'incoming-' + nodeId + '-' + si;
            html += '<button class="incoming-link-toggle" ' +
                'onclick="event.stopPropagation(); toggleIncomingLinks(\'' + panelId + '\')">' +
                escapeHtml(section.kind) + ' (' + section.links.length + ')</button>';
            html += '<div class="req-level-links-panel incoming-link-panel" id="' + panelId + '" style="display:none;">';
            section.links.forEach(function(link) {
                var st = link.state || {};
                var badge = '';
                if (st.label) {
                    var colorClass = st.color ? ' active val-' + st.color : '';
                    badge = '<span class="status-badge incoming-state-badge' + colorClass + '">' +
                        escapeHtml(st.label) + '</span>';
                }
                html += '<div class="assertion-test-item incoming-link-row" title="' +
                    escapeAttr(link.tooltip || '') + '">';
                html += badge;
                html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                    escapeAttr(link.id) + '\')">' +
                    escapeHtml(link.ref_id || link.id) + ' - ' + escapeHtml(link.title || '') + '</a>';
                html += '</div>';
            });
            html += '</div>';
        });
        html += '</div></div>';
    }

    // Structure section — assertions and body sections
    var allChildren = data.children || [];
    var assertions = allChildren.filter(function(c) { return c.kind === 'assertion'; });
    var remainders = allChildren.filter(function(c) { return c.kind === 'remainder' && c.heading; });

    if (editState.cardViewMode === 'complete') {
        // Complete view: interleave assertions and sections in document order
        var structItems = assertions.map(function(a) { return {type: 'assertion', data: a, order: a.render_order != null ? a.render_order : (a.line || 0)}; })
            .concat(remainders.map(function(r, i) { return {type: 'remainder', data: r, idx: i, order: r.render_order != null ? r.render_order : (r.line || 0)}; }));
        structItems.sort(function(a, b) { return a.order - b.order; });

        html += '<div class="card-section">';
        html += '<div class="card-assertions">';
        var assertionHeaderShown = false;
        structItems.forEach(function(item) {
            if (item.type === 'assertion') {
                if (!assertionHeaderShown) {
                    html += '<div class="card-section-label" style="margin-top:4px;">Assertions (' + assertions.length + ')</div>';
                    assertionHeaderShown = true;
                }
                html += buildAssertionHtml(nodeId, item.data, data);
            } else {
                html += buildRemainderHtml(nodeId, item.data, item.idx);
            }
        });
        html += '</div>';
        html += '<button class="card-add-assertion-btn edit-only" onclick="onAddAssertion(\'' + nodeId + '\')">+ Add Assertion</button>';
        html += '<button class="card-add-section-btn edit-only" onclick="onRemainderAdd(\'' + nodeId + '\')">+ Add Section</button>';
        html += '</div>';
    } else {
        // Compact view: assertions only
        if (assertions.length > 0) {
            html += '<div class="card-section"><div class="card-section-label">Assertions (' + assertions.length + ')</div>';
            html += '<div class="card-assertions">';
            assertions.forEach(function(a) {
                html += buildAssertionHtml(nodeId, a, data);
            });
            html += '</div>';
            html += '<button class="card-add-assertion-btn edit-only" onclick="onAddAssertion(\'' + nodeId + '\')">+ Add Assertion</button>';
            html += '</div>';
        } else {
            html += '<div class="card-section"><div class="card-section-label">Assertions</div>';
            html += '<div style="font-size:0.8rem;color:var(--secondary-text);">No assertions defined</div>';
            html += '<button class="card-add-assertion-btn edit-only" onclick="onAddAssertion(\'' + nodeId + '\')">+ Add Assertion</button>';
            html += '</div>';
        }
    }

    // Integrates — read-only relationship to external associate (library) REQs.
    // The INTEGRATES edge is spec-authored, external-only, and cross-repo, so it
    // is NOT user-editable here (see CUR-1419 Task 4 decision: not added to
    // _USER_RELATIONSHIP_KINDS). Surfaces on BOTH the consumer card (outgoing →
    // library) and the library card (incoming → consumer) since both carry the
    // edge in `links`. See REQ-d00252-D.
    var integratesLinks = (data.links || []).filter(function(l) { return l.edge_kind === 'integrates'; });
    var integratesIds = {};
    integratesLinks.forEach(function(l) { integratesIds[l.id] = true; });
    if (integratesLinks.length > 0) {
        html += '<div class="card-section integrates-refs"><div class="card-section-label">Integrates (' + integratesLinks.length + ')</div>';
        html += '<div class="card-parents">';
        integratesLinks.forEach(function(link) {
            html += '<div class="card-parent-row">';
            html += '<span class="card-parent-kind view-only">INTEGRATES</span>';
            html += '<a class="card-parent-link" href="#" onclick="event.preventDefault(); openCard(\'' + link.id + '\')">';
            html += escapeHtml(link.id) + ' - ' + escapeHtml(link.title || '');
            html += '</a>';
            html += '</div>';
        });
        html += '</div></div>';
    }

    // Children (requirements only). Exclude integrates targets: the library REQ
    // reached via an outgoing INTEGRATES edge surfaces above as an "Integrates"
    // relationship, not as a structural child (REQ-d00252-D).
    const childReqs = (data.children || []).filter(function(c) {
        return c.kind === 'requirement' && !integratesIds[c.id];
    });
    if (childReqs.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Children (' + childReqs.length + ')</div>';
        html += '<div class="card-children">';
        childReqs.forEach(function(c) {
            html += '<a class="card-child-link" href="#" onclick="event.preventDefault(); openCard(\'' + c.id + '\')">';
            html += c.id.replace('REQ-', '');
            if (c.title) html += ' - ' + escapeHtml(c.title);
            html += '</a>';
        });
        html += '</div></div>';
    }

    html += '</div>'; // end req-card-body
    html += '</div>'; // end req-card

    return html;
}

function updateNavSelection() {
    document.querySelectorAll('.nav-tree-row').forEach(function(row) {
        row.classList.toggle('selected', editState.openCards.has(row.dataset.id));
    });
}

// =====================================================================
// Journey Cards — reads from generic /api/node/ response
// =====================================================================

function buildJourneyCardHtml(nodeId, data, collapsed) {
    var collapsedClass = collapsed ? ' collapsed' : '';
    var focusedClass = editState.openCardOrder[0] === nodeId ? ' focused' : '';
    var title = escapeHtml(data.title || '');
    var shortId = escapeHtml(data.id || nodeId);
    var props = data.properties || {};
    var sourcePath = (data.source && data.source.path) || '';
    var sourceLine = (data.source && data.source.line) || 1;

    var html = '<div class="req-card journey-card' + collapsedClass + focusedClass + '" id="card-' + nodeId + '" data-journey-id="' + nodeId + '">';

    // Header — show journey ID + editable title
    html += '<div class="req-card-header" onclick="toggleCardCollapse(\'' + nodeId + '\')">';
    html += '<span class="req-card-expand">&#9660;</span>';
    html += '<span class="req-card-id">' + shortId + '</span>';
    html += '<span class="req-card-title-display view-only">' + title + '</span>';
    // Edit mode: inline title input
    html += '<input type="text" class="edit-only journey-title-edit" value="' + escapeAttr(data.title || '') + '"' +
        ' onclick="event.stopPropagation()" onblur="onJourneyTitleBlur(\'' + nodeId + '\', this)"' +
        ' style="font-size:0.9rem;font-weight:600;border:none;border-bottom:1px dashed var(--border-color);background:transparent;color:var(--input-text);flex:1;outline:none;">';
    // Edit mode: delete journey
    html += '<button class="edit-only journey-delete-btn" onclick="event.stopPropagation(); onDeleteJourney(\'' + nodeId + '\')" title="Delete journey">&#x1F5D1;</button>';
    html += '<button class="req-card-close" onclick="event.stopPropagation(); closeCard(\'' + nodeId + '\')" title="Close card">&times;</button>';
    html += '</div>';

    // Body
    html += '<div class="req-card-body">';

    // UAT verdict badge (pass/fail/partial/unverified)
    // Uses the same validation-* CSS classes as assertion test-result buttons.
    var verdict = props.verdict || 'unverified';
    if (verdict !== 'unverified' || props.verdict) {
        var verdictClassMap = {
            'pass': 'validation-pass',
            'fail': 'validation-fail',
            'partial': 'validation-partial',
            'unverified': 'validation-unknown'
        };
        var verdictClass = verdictClassMap[verdict] || 'validation-unknown';
        html += '<div class="card-section" style="padding:4px 12px 0;">';
        html += '<span class="' + verdictClass + ' active" style="font-size:0.65rem;padding:2px 7px;border-radius:3px;display:inline-block;" title="UAT Verdict">UAT: ' + escapeHtml(verdict.toUpperCase()) + '</span>';
        var failingSteps = props.failing_steps || [];
        if (verdict === 'fail' && failingSteps.length > 0) {
            html += ' <span style="font-size:0.7rem;color:var(--secondary-text);">Failing steps: ' + escapeHtml(failingSteps.join(', ')) + '</span>';
        }
        html += '</div>';
    }

    // Metadata section — source file right-justified on the label line
    var sourceLink = '';
    if (sourcePath) {
        sourceLink = '<a href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeJsInAttr(sourcePath) + '\', ' + sourceLine + ', \'' + nodeId + '\')" ' +
            'style="color:#0d6efd;cursor:pointer;font-size:0.7rem;text-decoration:none;text-transform:none;margin-left:auto;">' +
            escapeHtml(sourcePath.split('/').pop()) + ':' + sourceLine + '</a>';
        sourceLink += '<button class="edit-only journey-move-btn" onclick="event.preventDefault(); onMoveToFile(\'' + nodeId + '\')" style="margin-left:4px;">Move</button>';
    }
    html += '<div class="card-section" data-anchor="' + nodeId + '"><div class="card-section-label" style="display:flex;align-items:baseline;">Metadata' + sourceLink + '</div>';
    html += '<div class="card-meta">';

    // Actor — view + edit
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Actor:</span>';
    html += '<span class="card-meta-value journey-field-view">' + escapeHtml(props.actor || '') + '</span>';
    html += '<input type="text" class="edit-only journey-field-edit" value="' + escapeAttr(props.actor || '') + '"' +
        ' data-field="actor" data-journey-id="' + nodeId + '" onblur="onJourneyFieldBlur(this)"' +
        ' style="font-size:inherit;border:none;border-bottom:1px dashed var(--border-color);background:transparent;color:var(--input-text);width:90%;outline:none;">';
    html += '</div>';

    // Goal — view + edit
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Goal:</span>';
    html += '<span class="card-meta-value journey-field-view">' + escapeHtml(props.goal || '') + '</span>';
    html += '<input type="text" class="edit-only journey-field-edit" value="' + escapeAttr(props.goal || '') + '"' +
        ' data-field="goal" data-journey-id="' + nodeId + '" onblur="onJourneyFieldBlur(this)"' +
        ' style="font-size:inherit;border:none;border-bottom:1px dashed var(--border-color);background:transparent;color:var(--input-text);width:90%;outline:none;">';
    html += '</div>';

    if (props.descriptor) {
        html += '<div class="card-meta-item">';
        html += '<span class="card-meta-key">Topic:</span>';
        html += '<span class="card-meta-value">' + escapeHtml(props.descriptor) + '</span>';
        html += '</div>';
    }

    html += '</div></div>';

    // Context (view + edit) — styled like remainder sections (Steps, Expected Outcome)
    if (props.context || editState.enabled) {
        html += '<div class="card-body-section-wrapper">';
        html += '<div class="card-body-section-heading"><span>Context</span></div>';
        html += '<div class="card-body-section-content journey-field-view">' + simpleMarkdown(props.context || '', true) + '</div>';
        html += '<textarea class="edit-only journey-field-edit" data-field="context" data-journey-id="' + nodeId + '"' +
            ' onblur="onJourneyFieldBlur(this)"' +
            ' style="width:100%;min-height:120px;font-size:0.8rem;border:1px solid var(--border-color);border-radius:4px;padding:6px;resize:vertical;background:var(--card-bg);color:var(--input-text);">' +
            escapeHtml(props.context || '') + '</textarea>';
        html += '</div>';
    }

    // Validates — with assertion targets and edit controls (same pattern as
    // REQ parents). Collapsible like Incoming Links: one toggle, closed by
    // default (enrollment journeys can validate many assertions).
    var refs = (data.links || []).filter(function(l) { return l.edge_kind === 'validates'; });
    html += '<div class="card-section journey-refs"><div class="card-section-label">Validates</div>';
    html += '<div class="card-parents">';
    var validatesPanelId = 'jny-validates-' + nodeId;
    html += '<button class="incoming-link-toggle jny-validates-toggle" ' +
        'onclick="event.stopPropagation(); toggleIncomingLinks(\'' + validatesPanelId + '\')">' +
        refs.length + ' link(s)</button>';
    html += '<div class="req-level-links-panel jny-validates-panel" id="' + validatesPanelId + '" style="display:none;">';
    refs.forEach(function(link) {
        html += '<div class="card-parent-row">';
        var lTargetsJson = (link.assertion_targets && link.assertion_targets.length > 0)
            ? escapeAttr(JSON.stringify(link.assertion_targets)) : '';
        html += '<a class="card-parent-link" href="#" onclick="event.preventDefault(); openCardAndFlash(\'' + link.id + '\', ' +
            (lTargetsJson ? 'JSON.parse(this.dataset.targets)' : '[]') + ')"' +
            (lTargetsJson ? ' data-targets="' + lTargetsJson + '"' : '') + '>';
        html += link.id + ' - ' + escapeHtml(link.title || '');
        if (link.assertion_targets && link.assertion_targets.length > 0) {
            html += ' <span class="card-parent-targets">[' + link.assertion_targets.join(', ') + ']</span>';
        }
        html += '</a>';
        // Assertion targets picker button (edit mode)
        html += '<button class="card-parent-targets-btn edit-only" ' +
            'onclick="event.preventDefault(); editEdgeTargets(\'' + nodeId + '\', \'' + link.id + '\', this)" ' +
            'title="Edit assertion targets">&#9998;</button>';
        html += '<button class="card-parent-delete edit-only" ' +
            'onclick="event.preventDefault(); onDeleteEdge(\'' + nodeId + '\', \'' + link.id + '\')" ' +
            'title="Remove relationship">&times;</button>';
        html += '</div>';
    });
    html += '</div>'; // .jny-validates-panel
    html += '</div>'; // .card-parents
    html += '<button class="card-add-rel-btn edit-only" ' +
        'onclick="onAddJourneyRef(\'' + nodeId + '\', this)">+ Add Relationship</button>';
    html += '</div>';

    // Steps — rendered like REQ assertions: the step text plainly, with
    // right-aligned "Verified" (a test verifies the step) and result
    // ("Passed"/"Failed") badges that follow the shared assertion badge
    // full/abbrev/dot toggle (editState.assertionBadgeMode).
    var steps = (data.children || []).filter(function(c) { return c.kind === 'step'; });
    steps.sort(function(a, b) {
        var ao = a.render_order != null ? a.render_order : (a.line || 0);
        var bo = b.render_order != null ? b.render_order : (b.line || 0);
        return ao - bo;
    });
    if (steps.length > 0) {
        var stepBadgeMode = editState.assertionBadgeMode || 'abbrev';
        html += '<div class="card-section journey-steps"><div class="card-section-label">Steps (' + steps.length + ')</div>';
        steps.forEach(function(step) {
            var sStatus = step.status || 'untested';
            var stepNum = step.label || '';
            var vTests = step.verifying_tests || [];
            var verified = vTests.length > 0;

            // Row mirrors .card-assertion: number pill, plain text, badges right
            html += '<div class="journey-step-row card-assertion" data-step-status="' + sStatus + '">';
            html += '<span class="card-assertion-label journey-step-num">' + escapeHtml(stepNum) + '</span>';
            html += '<span class="card-assertion-text journey-step-text">' + escapeHtml(step.text || '') + '</span>';
            html += '<span class="assertion-badges-area">';

            // Result entries come from the verifying tests (a result implies a
            // verifier), so VER and Result are independent affordances shown
            // ONLY when their own evidence exists — suppressed otherwise, like
            // the assertion pills. A VERIFIES edge without a result -> VER only.
            var stepResults = [];
            vTests.forEach(function(t) { (t.results || []).forEach(function(r) { stepResults.push(r); }); });
            var hasResult = stepResults.length > 0 || sStatus === 'pass' || sStatus === 'fail';

            // VER badge — only when a test verifies this step. Toggles the
            // verifying-TESTS panel.
            if (verified) {
                var verLabel = stepBadgeMode === 'full' ? 'Verified' : (stepBadgeMode === 'dots' ? 'V' : 'VER');
                var verTip = vTests.length + (vTests.length === 1 ? ' test verifies' : ' tests verify') + ' this step';
                html += '<button class="journey-step-badge implemented-yes" title="' + escapeAttr(verTip) + '"' +
                    ' onclick="event.stopPropagation(); toggleJourneyStepPanel(this, \'journey-step-tests-' + nodeId + '-' + escapeAttr(stepNum) + '\')">' + verLabel + '</button>';
            }

            // Result badge — only when a result exists. Neutral "Result" label;
            // COLOR carries the outcome (green pass / red fail / grey
            // indeterminate), so a red badge reads as a failure without the label
            // contradicting it. Toggles the RESULTS panel (not the tests).
            if (hasResult) {
                var resCls = sStatus === 'pass' ? 'validation-pass' : (sStatus === 'fail' ? 'validation-fail' : 'validation-unknown');
                var resLabel = stepBadgeMode === 'full' ? 'Result' : (stepBadgeMode === 'dots' ? 'R' : 'RES');
                var resTip = sStatus === 'pass' ? 'Verifying test results passed'
                    : (sStatus === 'fail' ? 'A verifying test result failed' : 'Test results present (indeterminate)');
                html += '<button class="journey-step-badge ' + resCls + '" title="' + escapeAttr(resTip) + '"' +
                    ' onclick="event.stopPropagation(); toggleJourneyStepPanel(this, \'journey-step-results-' + nodeId + '-' + escapeAttr(stepNum) + '\')">' + resLabel + '</button>';
            }

            // No verifier at all -> a muted hint, mirroring the assertion "no
            // coverage" placeholder (a result can't exist without a verifier).
            if (!verified) {
                html += '<span class="assertion-no-coverage" title="No test verifies this step" style="font-size:0.6rem;color:var(--secondary-text);opacity:0.5;">no tests</span>';
            }

            html += '</span>'; // .assertion-badges-area
            html += '</div>'; // .journey-step-row

            // Verifying-TESTS panel (VER badge) — which tests verify this step.
            html += '<div class="journey-step-tests" id="journey-step-tests-' + nodeId + '-' + escapeAttr(stepNum) + '"' +
                ' style="display:none;margin-left:34px;margin-bottom:4px;">';
            vTests.forEach(function(t) {
                // Clean, non-duplicated display text: prefer the test name,
                // then repo-relative file:line, then the label/id fallback.
                var displayText = t.name
                    ? t.name
                    : (t.file ? (t.file + ':' + (t.line || 0)) : (t.label || t.id || ''));
                html += '<div class="journey-step-test-row" style="font-size:0.75rem;display:flex;align-items:center;gap:4px;padding:1px 0;">';
                html += '<span class="assertion-test-icon">&#x1F9EA;</span>';
                if (t.file) {
                    html += '<a class="journey-step-test-link" href="#" style="font-family:monospace;" onclick="event.preventDefault(); showSource(\'' +
                        escapeJsInAttr(t.file) + '\', ' + (t.line || 0) + ', \'' + escapeAttr(t.id || '') + '\')">' + escapeHtml(displayText) + '</a>';
                } else {
                    html += '<span class="journey-step-test-link" style="font-family:monospace;">' + escapeHtml(displayText) + '</span>';
                }
                html += '</div>';
            });
            html += '</div>';

            // RESULTS panel (Result badge) — the actual pass/fail result entries
            // produced by the verifying tests (distinct from the tests above).
            html += '<div class="journey-step-results" id="journey-step-results-' + nodeId + '-' + escapeAttr(stepNum) + '"' +
                ' style="display:none;margin-left:34px;margin-bottom:4px;">';
            stepResults.forEach(function(r) {
                var rs = (r.status || '').toLowerCase();
                var rIcon = (rs === 'passed' || rs === 'pass' || rs === 'success') ? '&#x2705;'
                    : ((rs === 'failed' || rs === 'fail' || rs === 'failure' || rs === 'error') ? '&#x274C;' : '&#x2753;');
                // Link to the results ARTIFACT (junit.xml:<testcase> line)
                // when known, not the test's source file.
                var rFile = r.result_file || r.file;
                var rLine = (r.result_line != null && r.result_line) || r.line || 0;
                var rText = (rFile ? (rFile + ':' + rLine) : (r.id || 'result')) +
                    (r.duration ? ' (' + r.duration + 's)' : '');
                html += '<div class="journey-step-result-row" style="font-size:0.75rem;display:flex;align-items:center;gap:4px;padding:1px 0;">';
                html += '<span class="assertion-test-icon">' + rIcon + '</span>';
                if (rFile) {
                    html += '<a class="journey-step-test-link" href="#" style="font-family:monospace;" onclick="event.preventDefault(); showSource(\'' +
                        escapeJsInAttr(rFile) + '\', ' + rLine + ', \'' + escapeAttr(r.id || '') + '\')">' + escapeHtml(rText) + '</a>';
                } else {
                    html += '<span class="journey-step-test-link" style="font-family:monospace;">' + escapeHtml(rText) + '</span>';
                }
                html += '</div>';
            });
            html += '</div>';
        });
        html += '</div>';
    }

    // Sections (view + edit) — skip the prose "Steps" section (rendered above)
    var sections = props.sections || [];
    html += '<div class="journey-sections" data-journey-id="' + nodeId + '">';
    sections.filter(function(sec) { return sec.name.toLowerCase() !== 'steps'; }).forEach(function(sec) {
        html += '<div class="card-body-section-wrapper journey-section" data-section-name="' + escapeAttr(sec.name) + '" data-anchor="' + nodeId + '#section:' + escapeAttr(sec.name) + '">';
        // Section header — view + edit
        html += '<div class="card-body-section-heading journey-section-header">';
        html += '<span class="journey-section-name-view">' + escapeHtml(sec.name) + '</span>';
        html += '<input type="text" class="edit-only journey-section-name-edit" value="' + escapeAttr(sec.name) + '"' +
            ' data-journey-id="' + nodeId + '" data-original-name="' + escapeAttr(sec.name) + '"' +
            ' onblur="onJourneySectionNameBlur(this)" style="font-weight:600;flex:1;">';
        html += '<button class="edit-only journey-section-delete" onclick="onDeleteJourneySection(\'' + nodeId + '\', \'' + escapeAttr(sec.name) + '\')" title="Delete section">&times;</button>';
        html += '</div>';
        // Section content — view + edit
        html += '<div class="card-body-section-content journey-section-content-view">' + simpleMarkdown(sec.content || '', true) + '</div>';
        html += '<textarea class="edit-only journey-section-content-edit" data-journey-id="' + nodeId + '"' +
            ' data-section-name="' + escapeAttr(sec.name) + '" onblur="onJourneySectionContentBlur(this)"' +
            ' style="width:100%;min-height:120px;font-size:0.8rem;border:1px solid var(--border-color);border-radius:4px;padding:6px;resize:vertical;background:var(--card-bg);color:var(--input-text);">' +
            escapeHtml(sec.content || '') + '</textarea>';
        html += '</div>';
    });
    html += '<button class="edit-only journey-section-add-btn" onclick="onAddJourneySection(\'' + nodeId + '\')">+ Add Section</button>';
    html += '</div>';

    html += '</div>'; // end req-card-body
    html += '</div>'; // end req-card

    return html;
}

// =====================================================================
// Generic Card — fallback for TEST, CODE, RESULT, etc.
// =====================================================================

function buildGenericCardHtml(nodeId, data, collapsed) {
    var collapsedClass = collapsed ? ' collapsed' : '';
    var focusedClass = editState.openCardOrder[0] === nodeId ? ' focused' : '';
    var title = escapeHtml(data.title || nodeId);
    var kind = data.kind || 'unknown';
    var kindLabel = kind.charAt(0).toUpperCase() + kind.slice(1);
    var props = data.properties || {};
    var sourcePath = (data.source && data.source.path) || '';
    var sourceLine = (data.source && data.source.line) || 0;

    var html = '<div class="req-card' + collapsedClass + focusedClass + '" id="card-' + nodeId + '">';

    // Header
    html += '<div class="req-card-header" onclick="toggleCardCollapse(\'' + nodeId + '\')">';
    html += '<span class="req-card-expand">&#9660;</span>';
    html += '<span class="req-card-id">' + escapeHtml(kindLabel) + '</span>';
    html += '<span class="req-card-title-display">' + title + '</span>';
    html += '<button class="req-card-close" onclick="event.stopPropagation(); closeCard(\'' + nodeId + '\')" title="Close card">&times;</button>';
    html += '</div>';

    // Body
    html += '<div class="req-card-body">';

    // Source
    if (sourcePath) {
        html += '<div class="card-section"><div class="card-section-label">Source</div>';
        html += '<div class="card-meta"><div class="card-meta-item">';
        html += '<a class="card-meta-value" href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeJsInAttr(sourcePath) + '\', ' + sourceLine + ', \'' + nodeId + '\')" ' +
            'style="color:#0d6efd;cursor:pointer;font-size:0.75rem;text-decoration:none;">' +
            escapeHtml(sourcePath.split('/').pop()) + ':' + sourceLine + '</a>';
        html += '</div></div></div>';
    }

    // Properties as key-value pairs
    var propKeys = Object.keys(props);
    if (propKeys.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Properties</div>';
        html += '<div class="card-meta">';
        propKeys.forEach(function(key) {
            var val = props[key];
            if (val === null || val === undefined || val === '') return;
            html += '<div class="card-meta-item">';
            html += '<span class="card-meta-key">' + escapeHtml(key) + ':</span>';
            html += '<span class="card-meta-value">' + escapeHtml(String(val)) + '</span>';
            html += '</div>';
        });
        html += '</div></div>';
    }

    // Links
    var links = data.links || [];
    if (links.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Links (' + links.length + ')</div>';
        html += '<div class="card-parents">';
        links.forEach(function(link) {
            html += '<a class="card-parent-link" href="#" onclick="event.preventDefault(); openCard(\'' + link.id + '\')">';
            html += '<span class="card-parent-kind">' + escapeHtml(link.edge_kind || '') + '</span> ';
            html += escapeHtml(link.id);
            if (link.title) html += ' - ' + escapeHtml(link.title);
            html += '</a>';
        });
        html += '</div></div>';
    }

    html += '</div>'; // end req-card-body
    html += '</div>'; // end req-card

    return html;
}

// =====================================================================
// Simple Markdown Renderer (for journey body text)
// =====================================================================

// Implements: REQ-d00245-A
function simpleMarkdown(text, annotateTerms) {
    // Phase 1: extract pipe tables into HTML chunks keyed by placeholder.
    // Cell content is escaped, bold-transformed, and term-annotated
    // here so the global term pass below skips it (placeholders are
    // non-word and survive escapeHtml).
    var cellTransform = function(cellText) {
        var safe = escapeHtml(cellText);
        safe = safe.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
        if (annotateTerms) {
            safe = _annotateTermsInString(safe);
        }
        return safe;
    };
    var extracted = extractMdTables(text.split('\n'), cellTransform);

    // Phase 2: run the existing line-by-line transforms over the
    // remaining (non-table) lines. Placeholder lines pass through
    // unchanged because they contain no markdown syntax.
    var result = [];
    for (var i = 0; i < extracted.lines.length; i++) {
        var line = escapeHtml(extracted.lines[i]);
        line = line.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
        line = line.replace(/^(#{1,3})\s+(.+)$/, '<strong>$2</strong>');
        line = line.replace(/^(\d+)\.\s+/, '<strong>$1.</strong> ');
        line = line.replace(/^(\s*)-\s+/, '$1&bull; ');
        result.push(line);
    }
    var output = result.join('\n');

    // Phase 3: term annotation on non-table content only.
    if (annotateTerms) {
        output = _annotateTermsInString(output);
    }

    // Phase 4: reinsert table HTML at placeholder positions.
    output = reinsertMdTables(output, extracted.chunks);

    return output;
}

function _annotateTermsInString(s) {
    if (!editState.termsLookup || Object.keys(editState.termsLookup).length === 0) {
        return s;
    }
    if (!editState.termsRegex) {
        _buildTermsRegex();
    }
    if (!editState.termsRegex) return s;
    return s.replace(editState.termsRegex, function(match) {
        var key = match.toLowerCase();
        var entry = editState.termsLookup[key];
        if (!entry) return match;
        var tip = entry.definition_short || entry.definition || '';
        if (tip.length > 80) tip = tip.substring(0, 80) + '...';
        return '<span class="defined-term" data-term-key="' + escapeAttr(key) +
            '" data-tip="' + escapeAttr(tip) + '">' + match + '</span>';
    });
}

function _buildTermsRegex() {
    var keys = Object.keys(editState.termsLookup);
    if (keys.length === 0) { editState.termsRegex = null; return; }
    // Sort longest-first to avoid partial matches
    keys.sort(function(a, b) { return b.length - a.length; });
    // Escape regex special chars and build alternation
    var escaped = keys.map(function(k) {
        return k.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    });
    // Word-boundary anchored, case-insensitive
    editState.termsRegex = new RegExp('\\b(' + escaped.join('|') + ')\\b', 'gi');
}

// Implements: REQ-d00245-B — delegated click handler for .defined-term spans.
// Scoped to document.body so clicks land for terms surfaced anywhere —
// card stack, file-viewer Rendered pane, etc.
document.addEventListener('DOMContentLoaded', function() {
    document.body.addEventListener('click', function(e) {
        var span = e.target.closest('.defined-term');
        if (span) {
            e.preventDefault();
            var key = span.getAttribute('data-term-key');
            if (key) openTermCard(key);
        }
    });
});

// =====================================================================
// Coverage Prefetch — warm the per-assertion panel caches on card open
// =====================================================================
//
// Badge COLORS no longer depend on this prefetch: they are baked into the
// rendered HTML from the server-computed assertion_coverage_states
// (REQ-d00258-G). This prefetch now only warms the caches the expand-panels
// reuse, so opening a Tests/Code/UAT panel is instant.

async function prefetchCoverage(nodeId) {
    var testData = assertionTestsCache[nodeId]
        ? Promise.resolve(assertionTestsCache[nodeId])
        : apiFetch('/api/test-coverage/' + encodeURIComponent(nodeId));
    var codeData = assertionCodeCache[nodeId]
        ? Promise.resolve(assertionCodeCache[nodeId])
        : apiFetch('/api/code-coverage/' + encodeURIComponent(nodeId));
    var uatData = assertionUatCache[nodeId]
        ? Promise.resolve(assertionUatCache[nodeId])
        : apiFetch('/api/uat-coverage/' + encodeURIComponent(nodeId));

    var results = await Promise.all([testData, codeData, uatData]);

    if (results[0] && results[0].success) {
        assertionTestsCache[nodeId] = results[0];
    }
    if (results[1] && results[1].success) {
        assertionCodeCache[nodeId] = results[1];
    }
    if (results[2] && results[2].success) {
        assertionUatCache[nodeId] = results[2];
    }
}

function setAssertionBadgeMode(mode) {
    editState.assertionBadgeMode = mode;
    renderCardStack();
}

var _linkStyleCycle = ['full', 'abbrev', 'dots'];
var _linkStyleLabels = {full: 'Link Style: Full', abbrev: 'Link Style: Abbrv', dots: 'Link Style: Dot'};

function cycleLinkStyle() {
    var current = editState.assertionBadgeMode || 'abbrev';
    var idx = _linkStyleCycle.indexOf(current);
    var next = _linkStyleCycle[(idx + 1) % _linkStyleCycle.length];
    editState.assertionBadgeMode = next;
    var btn = document.getElementById('assertion-badge-mode-btn');
    if (btn) btn.textContent = _linkStyleLabels[next];
    renderCardStack();
    saveState();
}

function toggleCardViewMode() {
    editState.cardViewMode = editState.cardViewMode === 'compact' ? 'complete' : 'compact';
    var btn = document.getElementById('card-view-mode');
    if (btn) btn.textContent = (editState.cardViewMode === 'compact' ? 'Compact' : 'Complete') + ' Card View';
    renderCardStack();
    saveState();
}

function toggleBodySection(nodeId, idx) {
    var content = document.getElementById('body-section-content-' + nodeId + '-' + idx);
    var toggle = document.getElementById('body-section-toggle-' + nodeId + '-' + idx);
    if (!content) return;
    var hidden = content.style.display === 'none';
    content.style.display = hidden ? '' : 'none';
    if (toggle) toggle.textContent = hidden ? '[-]' : '[+]';
}

// =====================================================================
// Assertion Validation — Test Coverage Buttons
// =====================================================================

var assertionTestsCache = {};

// Journey step badges each toggle their OWN pre-rendered panel: VER -> the
// verifying-tests panel, Result -> the results panel. (Data is embedded in the
// journey card payload — step.verifying_tests[].results — so no fetch needed.)
// Each badge owns exactly one panel; they are no longer a coupled pair.
function toggleJourneyStepPanel(btn, panelId) {
    var panel = document.getElementById(panelId);
    if (!panel) return;
    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }
    panel.style.display = 'block';
    btn.classList.add('active');
}

async function toggleAssertionTests(btn, reqId, label) {
    var panelId = 'assertion-tests-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    // Toggle visibility
    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    // Fetch data if not cached
    if (!assertionTestsCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/test-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load coverage data</div>';
            return;
        }
        assertionTestsCache[reqId] = data;
    }

    renderAssertionTests(panel, reqId, label, assertionTestsCache[reqId]);
}

function renderAssertionTests(panel, reqId, label, data) {
    var assertionData = data.assertion_tests[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var tests = assertionData.tests;
    if (tests.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No tests cover this assertion</div>';
        return;
    }

    var html = '';
    for (var i = 0; i < tests.length; i++) {
        var t = tests[i];
        var statusIcon = '';
        var statusClass = '';
        // Determine overall status from results
        var hasResults = t.results && t.results.length > 0;
        if (hasResults) {
            var allPassed = t.results.every(function(r) { return r.status === 'passed'; });
            var anyFailed = t.results.some(function(r) { return r.status === 'failed' || r.status === 'failure' || r.status === 'error'; });
            if (allPassed) { statusIcon = '&#x2705;'; statusClass = 'test-passed'; }
            else if (anyFailed) { statusIcon = '&#x274C;'; statusClass = 'test-failed'; }
            else { statusIcon = '&#x2753;'; statusClass = ''; }
        } else {
            statusIcon = '&#x2796;'; statusClass = 'test-no-result';
        }

        var testLabel = escapeHtml(t.label || t.id);

        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        if (t.file) {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeJsInAttr(t.file) + '\', ' + (t.line || 0) + ', \'' + escapeAttr(t.id || '') + '\')">' + testLabel + '</a>';
        } else {
            html += '<span class="assertion-test-link">' + testLabel + '</span>';
        }
        // Result file link (e.g., test-results.xml)
        if (hasResults && t.results[0].file) {
            html += ' <a class="assertion-result-file-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeJsInAttr(t.results[0].file) + '\', ' + (t.results[0].line || 0) +
                ', \'' + escapeAttr(t.id || '') + '\')" title="View result in ' + escapeHtml(t.results[0].file) + '">[result]</a>';
        }
        html += '</div>';
    }
    panel.innerHTML = html;
}

// =====================================================================
// Assertion Results — Test Result Files (VER badge)
// =====================================================================

async function toggleAssertionResults(btn, reqId, label) {
    var panelId = 'assertion-results-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    // Fetch test data if not cached (results come from the same endpoint)
    if (!assertionTestsCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/test-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load results data</div>';
            return;
        }
        assertionTestsCache[reqId] = data;
    }

    renderAssertionResults(panel, reqId, label, assertionTestsCache[reqId]);
}

function renderAssertionResults(panel, reqId, label, data) {
    var assertionData = data.assertion_tests[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    // Collect all results across all tests
    var resultItems = [];
    var tests = assertionData.tests;
    for (var i = 0; i < tests.length; i++) {
        var t = tests[i];
        if (t.results && t.results.length > 0) {
            for (var j = 0; j < t.results.length; j++) {
                resultItems.push({ result: t.results[j], testLabel: t.label || t.id, testId: t.id });
            }
        }
    }

    if (resultItems.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No test results for this assertion</div>';
        return;
    }

    var html = '';
    for (var k = 0; k < resultItems.length; k++) {
        var r = resultItems[k].result;
        var tLabel = resultItems[k].testLabel;
        var status = (r.status || '').toLowerCase();
        var statusIcon = '';
        var statusClass = '';
        if (status === 'passed' || status === 'pass' || status === 'success') {
            statusIcon = '&#x2705;'; statusClass = 'test-passed';
        } else if (status === 'failed' || status === 'fail' || status === 'failure' || status === 'error') {
            statusIcon = '&#x274C;'; statusClass = 'test-failed';
        } else {
            statusIcon = '&#x2753;'; statusClass = '';
        }

        // Prefer the results-artifact location (result_file/result_line)
        // over the test's source file, mirroring the journey-step panel.
        var rFile = r.result_file || r.file;
        var rLine = (r.result_line != null && r.result_line) || r.line || 0;
        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        if (rFile) {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeJsInAttr(rFile) + '\', ' + rLine + ', \'' + escapeAttr(resultItems[k].testId || '') + '\')">' +
                escapeHtml(tLabel) + ' [' + escapeHtml(r.status) + ']</a>';
        } else {
            html += '<span class="assertion-test-link">' + escapeHtml(tLabel) + ' [' + escapeHtml(r.status) + ']</span>';
        }
        html += '</div>';
    }
    panel.innerHTML = html;
}

// Per-assertion Tested / Passing badge colors are server-computed
// (assertion_coverage_states) and baked in at render time (REQ-d00258-G); no
// client-side recolor pass is needed here anymore.

// =====================================================================
// Assertion Implementation — Code Coverage Buttons
// =====================================================================

var assertionCodeCache = {};

async function toggleAssertionCode(btn, reqId, label, edgeKind) {
    var panelId = 'assertion-code-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    // Toggle visibility
    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    var cacheKey = reqId + ':' + (edgeKind || 'all');

    // Fetch data if not cached
    if (!assertionCodeCache[cacheKey]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var url = '/api/code-coverage/' + encodeURIComponent(reqId);
        if (edgeKind) url += '?kind=' + edgeKind;
        var data = await apiFetch(url);
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load implementation data</div>';
            return;
        }
        assertionCodeCache[cacheKey] = data;
    }

    renderAssertionCode(panel, reqId, label, assertionCodeCache[cacheKey]);
}

function renderCodeRefItem(c) {
    // Extract short filename from full path
    var fileName = c.file ? c.file.split('/').pop() : c.id;

    var html = '<div class="assertion-test-item">';
    html += '<span class="assertion-test-icon">&#x1F4C4;</span>';
    if (c.file) {
        html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeJsInAttr(c.file) + '\', ' + (c.line || 0) + ', \'' + escapeAttr(c.id || '') + '\')">' +
            escapeHtml(fileName) + ':' + (c.line || 1) + '</a>';
    } else {
        html += '<span class="assertion-test-link">' + escapeHtml(c.label || c.id) + '</span>';
    }
    html += '</div>';
    return html;
}

function renderAssertionCode(panel, reqId, label, data) {
    var assertionData = data.assertion_code[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var codeRefs = assertionData.code_refs || [];
    var wholeReqRefs = assertionData.whole_req_code_refs || [];
    var refinesRefs = assertionData.refines_refs || [];

    if (codeRefs.length === 0 && wholeReqRefs.length === 0 && refinesRefs.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No references for this assertion</div>';
        return;
    }

    var html = '';
    for (var i = 0; i < codeRefs.length; i++) {
        html += renderCodeRefItem(codeRefs[i]);
    }

    if (wholeReqRefs.length > 0) {
        html += '<div class="assertion-test-subheader">Whole-requirement references (~)</div>';
        for (var j = 0; j < wholeReqRefs.length; j++) {
            html += renderCodeRefItem(wholeReqRefs[j]);
        }
    }

    if (refinesRefs.length > 0) {
        html += '<div class="assertion-test-subheader">Implemented via refinement (~)</div>';
        for (var k = 0; k < refinesRefs.length; k++) {
            var r = refinesRefs[k];
            html += '<div class="assertion-test-item">';
            html += '<span class="assertion-test-icon">&#x1F517;</span>';
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                escapeAttr(r.id) + '\')">' + escapeHtml(r.id) +
                (r.title ? ' — ' + escapeHtml(r.title) : '') + '</a>';
            html += '</div>';
        }
    }

    panel.innerHTML = html;
}

// =====================================================================
// Assertion Refines — Refining Requirement Buttons
// =====================================================================

var assertionRefinesCache = {};

async function toggleAssertionRefines(btn, reqId, label) {
    var panelId = 'assertion-refines-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    if (!assertionRefinesCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/refines-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load refines data</div>';
            return;
        }
        assertionRefinesCache[reqId] = data;
    }

    renderAssertionRefines(panel, reqId, label, assertionRefinesCache[reqId]);
}

function renderAssertionRefines(panel, reqId, label, data) {
    var assertionData = data.assertion_refines[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var refs = assertionData.refines_refs;
    if (refs.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No requirements refine this assertion</div>';
        return;
    }

    var html = '';
    for (var i = 0; i < refs.length; i++) {
        var r = refs[i];
        html += '<div class="assertion-test-item">';
        html += '<span class="assertion-test-icon">&#x1F517;</span>';
        html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
            escapeHtml(r.id) + '\')">' +
            escapeHtml(r.id) + ': ' + escapeHtml(r.title) + '</a>';
        html += '</div>';
    }
    panel.innerHTML = html;
}

// =====================================================================
// Assertion UAT — Journey Coverage Buttons (UAT Covered + UAT Passed).
// Badge colors come from the server (assertion_coverage_states); these
// toggles only open the per-assertion journey/result panels.
// =====================================================================

var assertionUatCache = {};

async function toggleAssertionUat(btn, reqId, label) {
    var panelId = 'assertion-uat-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    // Toggle visibility
    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    // Fetch data if not cached
    if (!assertionUatCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/uat-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load UAT data</div>';
            return;
        }
        assertionUatCache[reqId] = data;
    }

    renderAssertionUat(panel, reqId, label, assertionUatCache[reqId]);
}

function renderAssertionUat(panel, reqId, label, data) {
    var assertionData = data.assertion_journeys[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var journeys = assertionData.journeys;
    if (journeys.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No journeys validate this assertion</div>';
        return;
    }

    // Reuse the same rendering as tests (journeys have same structure)
    var html = '';
    for (var i = 0; i < journeys.length; i++) {
        var j = journeys[i];
        var statusIcon = '';
        var statusClass = '';
        var hasResults = j.results && j.results.length > 0;
        if (hasResults) {
            var allPassed = j.results.every(function(r) { return r.status === 'passed'; });
            var anyFailed = j.results.some(function(r) { return r.status === 'failed' || r.status === 'failure' || r.status === 'error'; });
            if (allPassed) { statusIcon = '&#x2705;'; statusClass = 'test-passed'; }
            else if (anyFailed) { statusIcon = '&#x274C;'; statusClass = 'test-failed'; }
            else { statusIcon = '&#x2753;'; statusClass = ''; }
        } else {
            statusIcon = '&#x2796;'; statusClass = 'test-no-result';
        }

        var jLabel = escapeHtml(j.label || j.id);
        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        // Journey links open the journey card instead of showing source
        html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
            escapeHtml(j.id) + '\')">' + jLabel + '</a>';
        html += '</div>';
    }
    panel.innerHTML = html;
}

// =====================================================================
// Assertion UAT Results — Journey Result Files (ACC badge)
// =====================================================================

async function toggleAssertionUatResults(btn, reqId, label) {
    var panelId = 'assertion-uat-results-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    if (!assertionUatCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/uat-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load UAT results</div>';
            return;
        }
        assertionUatCache[reqId] = data;
    }

    renderAssertionUatResults(panel, reqId, label, assertionUatCache[reqId]);
}

function renderAssertionUatResults(panel, reqId, label, data) {
    var assertionData = data.assertion_journeys[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var resultItems = [];
    var verdictLines = [];
    var journeys = assertionData.journeys;
    for (var i = 0; i < journeys.length; i++) {
        var j = journeys[i];
        // Journey verdict headline (e.g. "5/7 steps verified") — shown above
        // the result rows so a partial pill is explained even at a glance.
        if (j.total_steps) {
            verdictLines.push({ id: j.id, label: j.label || j.id,
                text: j.verified_steps + '/' + j.total_steps + ' steps verified (' + (j.tier || '') + ')' });
        }
        if (j.results && j.results.length > 0) {
            for (var k = 0; k < j.results.length; k++) {
                resultItems.push({ result: j.results[k], jnyLabel: j.label || j.id, jnyId: j.id });
            }
        }
    }

    if (resultItems.length === 0 && verdictLines.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No journey results for this assertion</div>';
        return;
    }

    var html = '';
    for (var v = 0; v < verdictLines.length; v++) {
        html += '<div class="assertion-test-item uat-journey-verdict">' +
            '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
            escapeAttr(verdictLines[v].id) + '\')">' + escapeHtml(verdictLines[v].label) + '</a> — ' +
            escapeHtml(verdictLines[v].text) + '</div>';
    }
    for (var r = 0; r < resultItems.length; r++) {
        var res = resultItems[r].result;
        var jLabel = resultItems[r].jnyLabel;
        var status = (res.status || '').toLowerCase();
        var statusIcon = '', statusClass = '';
        if (status === 'passed' || status === 'pass' || status === 'success') {
            statusIcon = '&#x2705;'; statusClass = 'test-passed';
        } else if (status === 'failed' || status === 'fail' || status === 'failure' || status === 'error') {
            statusIcon = '&#x274C;'; statusClass = 'test-failed';
        } else {
            statusIcon = '&#x2753;';
        }
        // Link to the results ARTIFACT when known (result_file/result_line),
        // mirroring the journey-step and test result panels.
        var rFile = res.result_file || res.file;
        var rLine = (res.result_line != null && res.result_line) || res.line || 0;
        var rowLabel = jLabel + (res.step ? ' step ' + res.step : '');

        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        if (rFile) {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeJsInAttr(rFile) + '\', ' + rLine + ', \'' + escapeAttr(resultItems[r].jnyId || '') + '\')">' +
                escapeHtml(rowLabel) + ' [' + escapeHtml(res.status) + ']</a>';
        } else {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                escapeHtml(resultItems[r].jnyId) + '\')">' +
                escapeHtml(rowLabel) + ' [' + escapeHtml(res.status) + ']</a>';
        }
        html += '</div>';
    }
    panel.innerHTML = html;
}

// =====================================================================
// REQ-Level Link Expansion (header badge click)
// =====================================================================

function toggleIncomingLinks(panelId) {
    // Show/hide a single Incoming Links kind panel (collapsed by default).
    var panel = document.getElementById(panelId);
    if (!panel) return;
    panel.style.display = (panel.style.display === 'none') ? 'block' : 'none';
}

function toggleReqLevelLinks(badge, reqId, dimKey, assertLinkKey) {
    var panelId = 'req-links-' + reqId + '-' + dimKey;
    var panel = document.getElementById(panelId);

    if (panel && panel.style.display !== 'none') {
        // Closing — just hide, no flash/scroll
        panel.style.display = 'none';
        return;
    }

    // Opening — flash assertions that have direct links
    _flashAssertions(reqId, assertLinkKey || dimKey);

    if (!panel) return;

    // Find the card data from editState
    var cardState = editState.openCards.get(reqId);
    if (!cardState || !cardState.data || !cardState.data.req_level_links) {
        return; // no panel to show, flash was enough
    }

    var links = cardState.data.req_level_links[dimKey] || [];
    // The IMPLEMENTED header badge folds in refinement provenance: a
    // requirement implemented via a Refines: (rather than directly) lists the
    // refining requirement(s) under "Implemented via refinement", mirroring the
    // per-assertion IMP pill. No standalone REFINED badge (REQ-d00069-L).
    var refinedLinks = (dimKey === 'implemented')
        ? (cardState.data.req_level_links['refined'] || [])
        : [];
    if (links.length === 0 && refinedLinks.length === 0) {
        return; // no REQ-level links, flash was enough
    }

    function _renderReqLinkItems(items) {
        var out = '';
        for (var i = 0; i < items.length; i++) {
            var link = items[i];
            // Result status icon for TEST/JNY nodes
            var statusIcon = '';
            if (link.result_status === 'passed') statusIcon = '&#x2705; ';
            else if (link.result_status === 'failed') statusIcon = '&#x274C; ';
            else if (link.result_status === 'mixed') statusIcon = '&#x26A0; ';

            var statusClass = '';
            if (link.result_status === 'failed') statusClass = ' test-failed';
            else if (link.result_status === 'passed') statusClass = ' test-passed';

            if (link.kind === 'journey') {
                out += '<div class="assertion-test-item' + statusClass + '">';
                out += '<span class="assertion-test-icon">' + statusIcon + '&#x1F4D6;</span>';
                out += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                    escapeHtml(link.id) + '\')">' + escapeHtml(link.title) + '</a>';
                out += '</div>';
            } else {
                var icon = link.kind === 'requirement' ? '&#x1F4CB;' : (link.kind === 'test' ? '&#x1F9EA;' : '&#x1F4C4;');
                out += '<div class="assertion-test-item' + statusClass + '">';
                out += '<span class="assertion-test-icon">' + statusIcon + icon + '</span>';
                if (link.file) {
                    out += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                        escapeJsInAttr(link.file) + '\', ' + (link.line || 0) + ', \'' + escapeAttr(link.id || '') + '\')">' + escapeHtml(link.title) + '</a>';
                } else {
                    out += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                        escapeHtml(link.id) + '\')">' + escapeHtml(link.title) + '</a>';
                }
                out += '</div>';
            }
        }
        return out;
    }

    var html = _renderReqLinkItems(links);
    if (refinedLinks.length > 0) {
        html += '<div style="font-style:italic;color:var(--secondary-text);font-size:0.72rem;margin-top:' +
            (links.length ? '6px' : '0') + ';">Implemented via refinement (~)</div>';
        html += _renderReqLinkItems(refinedLinks);
    }
    panel.innerHTML = html;
    panel.style.display = 'block';
}

function _flashAssertionsByLabel(reqId, labels) {
    // Flash specific assertion labels (e.g., ["A", "B"]) on a card
    if (!labels || labels.length === 0) return;
    var card = document.getElementById('card-' + reqId);
    if (!card) return;

    var targets = [];
    var wrappers = card.querySelectorAll('.card-assertion-wrapper');
    for (var i = 0; i < wrappers.length; i++) {
        var assertionDiv = wrappers[i].querySelector('.card-assertion');
        if (!assertionDiv) continue;
        var labelEl = assertionDiv.querySelector('.card-assertion-label');
        if (labelEl && labels.indexOf(labelEl.textContent.trim()) >= 0) {
            targets.push(assertionDiv);
        }
    }

    if (targets.length > 0) {
        targets[0].scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }
    setTimeout(function() {
        for (var j = 0; j < targets.length; j++) {
            targets[j].style.transition = 'none';
            targets[j].style.backgroundColor = 'rgba(13, 110, 253, 0.3)';
        }
        setTimeout(function() {
            for (var j = 0; j < targets.length; j++) {
                targets[j].style.transition = 'background-color 1s ease-out';
                targets[j].style.backgroundColor = '';
            }
        }, 400);
    }, 50);
}

async function openCardAndFlash(targetId, assertionTargets) {
    await openCard(targetId);
    if (assertionTargets && assertionTargets.length > 0) {
        // Small delay to ensure card DOM is rendered
        setTimeout(function() { _flashAssertionsByLabel(targetId, assertionTargets); }, 150);
    }
}

function _flashAssertions(reqId, assertLinkKey) {
    // Find card data to get assertion_links
    var cardState = editState.openCards.get(reqId);
    if (!cardState || !cardState.data || !cardState.data.assertion_links) return;

    var aLinks = cardState.data.assertion_links;
    var card = document.getElementById('card-' + reqId);
    if (!card) return;

    // Collect matching .card-assertion elements (they have the visible background)
    var targets = [];
    for (var label in aLinks) {
        if (!aLinks[label][assertLinkKey]) continue;
        var wrappers = card.querySelectorAll('.card-assertion-wrapper');
        for (var i = 0; i < wrappers.length; i++) {
            var assertionDiv = wrappers[i].querySelector('.card-assertion');
            if (!assertionDiv) continue;
            var labelEl = assertionDiv.querySelector('.card-assertion-label');
            if (labelEl && labelEl.textContent.trim() === label) {
                targets.push(assertionDiv);
            }
        }
    }

    if (targets.length > 0) {
        targets[0].scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }

    // Use inline style flash (same pattern as file-viewer line flash)
    setTimeout(function() {
        for (var j = 0; j < targets.length; j++) {
            targets[j].style.transition = 'none';
            targets[j].style.backgroundColor = 'rgba(13, 110, 253, 0.3)';
        }
        setTimeout(function() {
            for (var j = 0; j < targets.length; j++) {
                targets[j].style.transition = 'background-color 1s ease-out';
                targets[j].style.backgroundColor = '';
            }
        }, 400);
    }, 50);
}

// =====================================================================
// Term Cards (Implements: REQ-d00244-A, REQ-d00244-B, REQ-d00244-C)
// =====================================================================

async function openTermCard(termKey) {
    var cardId = 'term:' + termKey.toLowerCase();
    editState.focusNodeId = cardId;

    if (editState.openCards.has(cardId)) {
        focusCard(cardId);
        return;
    }

    var data = await apiFetch('/api/term/' + encodeURIComponent(termKey));
    if (!data) return;

    editState.openCards.set(cardId, { data: data, collapsed: false, kind: 'term', refsCollapsed: data.references && data.references.length > 20 });
    editState.openCardOrder = editState.openCardOrder.filter(function(id) { return id !== cardId; });
    editState.openCardOrder.unshift(cardId);

    renderCardStack();
    focusCard(cardId);
    updateNavSelection();
    saveState();
}

function toggleTermRefs(cardId) {
    var cardState = editState.openCards.get(cardId);
    if (!cardState) return;
    cardState.refsCollapsed = !cardState.refsCollapsed;
    var el = document.getElementById('term-refs-' + cardId);
    if (el) el.style.display = cardState.refsCollapsed ? 'none' : 'block';
    saveState();
}

function buildTermCardHtml(cardId, data, collapsed) {
    var collapsedClass = collapsed ? ' collapsed' : '';
    var focusedClass = editState.openCardOrder[0] === cardId ? ' focused' : '';
    var termName = escapeHtml(data.term || '');
    var collectionBadge = data.collection ? ' <span class="term-collection-badge">Collection</span>' : '';

    var html = '<div class="req-card term-card' + collapsedClass + focusedClass + '" id="card-' + escapeAttr(cardId) + '">';

    // Header (read-only, no edit controls)
    html += '<div class="req-card-header" onclick="toggleCardCollapse(\'' + escapeAttr(cardId) + '\')">';
    html += '<span class="req-card-expand">&#9660;</span>';
    html += '<span class="req-card-id" style="color:var(--secondary-text);">Term</span>';
    html += '<span class="req-card-title-display">' + termName + collectionBadge + '</span>';
    html += '<button class="req-card-close" onclick="event.stopPropagation(); closeCard(\'' + escapeAttr(cardId) + '\')" title="Close card">&times;</button>';
    html += '</div>';

    // Body
    html += '<div class="req-card-body">';

    // Definition section
    html += '<div class="card-section"><div class="card-section-label">Definition</div>';
    html += '<div class="term-definition">' + simpleMarkdown(data.definition || '') + '</div>';
    html += '</div>';

    // Citation section for reference-type terms
    if (data.is_reference && data.reference_fields) {
        var rf = data.reference_fields;
        html += '<div class="term-citation">';
        if (rf.title) {
            html += '<p class="term-citation-title">' + escapeHtml(rf.title) + '</p>';
        }
        if (rf.version) {
            html += '<p><strong>Version:</strong> ' + escapeHtml(rf.version) + '</p>';
        }
        if (rf.effective_date) {
            html += '<p><strong>Effective Date:</strong> ' + escapeHtml(rf.effective_date) + '</p>';
        }
        if (rf.url) {
            html += '<p><a href="' + escapeAttr(rf.url) + '" target="_blank">' + escapeHtml(rf.url) + '</a></p>';
        }
        html += '</div>';
    }

    // Reference term (synonym) display
    if (data.reference_term) {
        html += '<div class="term-synonym"><em>Reference Term:</em> ' + escapeHtml(data.reference_term);
        if (data.reference_source) {
            html += ' <span class="term-synonym-source">(' + escapeHtml(data.reference_source) + ')</span>';
        }
        html += '</div>';
    }

    // Source section
    html += '<div class="card-section"><div class="card-section-label">Source</div>';
    html += '<div class="card-meta">';
    // `defined_in` is a node id (REQUIREMENT or FILE); the server resolves it
    // into a concrete (defined_in_path, defined_in_line) pair so we can open
    // the file viewer directly. `repo_name` disambiguates FILE-id collisions
    // across federated repos (which `repo_root_for` cannot do on its own).
    if (data.defined_in_path) {
        var definedInPath = data.defined_in_path;
        var definedLine = data.defined_in_line || 1;
        html += '<div class="card-meta-item"><span class="card-meta-key">Defined in:</span>';
        html += '<a class="card-meta-value" href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeJsInAttr(definedInPath) + '\', ' + definedLine + ', \'' +
            escapeJsInAttr(data.defined_in || '') + '\', \'' +
            escapeJsInAttr(data.repo_name || '') + '\')">' + escapeHtml(definedInPath) + '</a>';
        html += '</div>';
    }
    if (data.namespace) {
        html += '<div class="card-meta-item"><span class="card-meta-key">Namespace:</span>';
        html += '<span class="card-meta-value">' + escapeHtml(data.namespace) + '</span>';
        html += '</div>';
    }
    html += '</div></div>';

    // References section (collapsible). Skipped entirely for terms marked
    // `Indexed: false`. Such terms are typically ubiquitous (e.g. "Assertion"
    // appearing thousands of times) where a per-card reference list adds
    // noise without value. The flag also stops the scanner from collecting
    // plain-word references (term_scanner.py:282-285), so hiding the panel
    // is consistent with the data model.
    if (data.indexed === false) {
        // Close req-card-body and req-card and return early.
        html += '</div></div>';
        return html;
    }
    var refs = data.references || [];
    var refSectionId = 'term-refs-' + escapeAttr(cardId);
    html += '<div class="card-section term-references">';
    var refsHidden = false;
    var cardState = editState.openCards.get(cardId);
    if (cardState && cardState.refsCollapsed !== undefined) {
        refsHidden = cardState.refsCollapsed;
    } else {
        refsHidden = refs.length > 20;
    }
    html += '<div class="card-section-label" style="cursor:pointer;user-select:none;" onclick="toggleTermRefs(\'' + escapeAttr(cardId) + '\')">';
    html += '<span style="font-size:0.7rem;margin-right:4px;">&#9660;</span>References (' + refs.length + ')</div>';
    html += '<div id="' + refSectionId + '"' + (refsHidden ? ' style="display:none"' : '') + '>';
    if (refs.length === 0) {
        html += '<div style="padding:8px;color:var(--secondary-text);font-size:0.8rem;font-style:italic;">No references resolved yet</div>';
    } else {
        // Separate file refs from non-file refs, group file refs by path
        var fileGroups = {};  // file_path -> [{line, fp}]
        var fileGroupOrder = [];
        var nonFileRefs = [];
        for (var ri = 0; ri < refs.length; ri++) {
            var ref = refs[ri];
            var nid = ref.node_id;
            var filePath = ref.file_path;
            var fileMatch = nid.match(/^(?:rem|code|test|file):(.+?)(?::(\d+))?$/);
            if (fileMatch || filePath) {
                var fp = filePath || fileMatch[1];
                if (!fileGroups[fp]) { fileGroups[fp] = []; fileGroupOrder.push(fp); }
                fileGroups[fp].push({ line: ref.line || 1, ref: ref });
            } else {
                nonFileRefs.push(ref);
            }
        }
        // Sort non-file refs by node_id then line number numerically
        nonFileRefs.sort(function(a, b) {
            if (a.node_id < b.node_id) return -1;
            if (a.node_id > b.node_id) return 1;
            return (a.line || 0) - (b.line || 0);
        });
        // Sort file groups by path
        fileGroupOrder.sort();

        html += '<div class="term-ref-compact" style="padding:6px 8px;line-height:1.8;">';
        // Non-file refs (requirements, assertions, journeys)
        for (var ri = 0; ri < nonFileRefs.length; ri++) {
            var ref = nonFileRefs[ri];
            var nid = ref.node_id;
            var ncClass = ref.canonical === false ? ' term-ref-noncanonical' : '';
            if (ref.parent_req_id) {
                var assertLabel = nid.substring(ref.parent_req_id.length + 1);
                html += '<a class="term-ref-chip' + ncClass + '" href="#" onclick="event.preventDefault();openCardAndFlash(\'' + escapeAttr(ref.parent_req_id) + '\',[\'' + escapeAttr(assertLabel) + '\'])"';
                html += ' title="' + escapeAttr(nid + (ref.node_title ? ' - ' + ref.node_title : '') + ' L' + ref.line) + '">';
                html += escapeHtml(nid);
                html += '<sub>' + ref.line + '</sub>';
                html += '</a>';
            } else {
                html += '<a class="term-ref-chip' + ncClass + '" href="#" onclick="event.preventDefault();openCard(\'' + escapeAttr(nid) + '\')"';
                html += ' title="' + escapeAttr(nid + (ref.node_title ? ' - ' + ref.node_title : '') + ' L' + ref.line) + '">';
                html += escapeHtml(nid);
                html += '<sub>' + ref.line + '</sub>';
                html += '</a>';
            }
        }
        // File refs grouped by path: each on its own line, path once then line numbers as links
        for (var fi = 0; fi < fileGroupOrder.length; fi++) {
            var fp = fileGroupOrder[fi];
            var lines = fileGroups[fp].slice().sort(function(a, b) { return a.line - b.line; });
            html += '<div class="term-ref-file-group">';
            html += '<span class="term-ref-chip term-ref-file" style="cursor:default;">';
            html += escapeHtml(fp) + '</span>';
            for (var li = 0; li < lines.length; li++) {
                var ln = lines[li].line;
                var lnNcClass = lines[li].ref && lines[li].ref.canonical === false ? ' term-ref-noncanonical' : '';
                html += '<a class="term-ref-file-line' + lnNcClass + '" href="#" onclick="event.preventDefault();showSource(\'' + escapeJsInAttr(fp) + '\',' + ln + ')"';
                html += ' title="' + escapeAttr(fp + ':' + ln) + '">';
                html += ln + '</a>';
                if (li < lines.length - 1) html += ', ';
            }
            html += '</div>';
        }
        html += '</div>';
    }
    html += '</div></div>';

    html += '</div>'; // end req-card-body
    html += '</div>'; // end req-card

    return html;
}
