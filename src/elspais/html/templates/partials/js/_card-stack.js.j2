// Implements: REQ-p00006-B
// Partial: _card-stack.js.j2 — Card rendering, open/close

// =====================================================================
// Card Stack — Unified for all node kinds
// =====================================================================

async function openCard(nodeId) {
    // If already open, just focus it
    if (editState.openCards.has(nodeId)) {
        focusCard(nodeId);
        return;
    }

    // Fetch node data via generic endpoint
    const data = await apiFetch('/api/node/' + encodeURIComponent(nodeId));
    if (!data) return;

    // Store in state with kind from response
    editState.openCards.set(nodeId, { data: data, collapsed: false, kind: data.kind });

    // Add to front of order
    editState.openCardOrder = editState.openCardOrder.filter(function(id) { return id !== nodeId; });
    editState.openCardOrder.unshift(nodeId);

    renderCardStack();
    // Prefetch coverage data so buttons are colored immediately
    if (data.kind === 'requirement') { prefetchCoverage(nodeId); }
    focusCard(nodeId);
    updateNavSelection();
    saveState();
}

function closeCard(nodeId) {
    editState.openCards.delete(nodeId);
    editState.openCardOrder = editState.openCardOrder.filter(function(id) { return id !== nodeId; });
    renderCardStack();
    updateNavSelection();
    saveState();
}

function focusCard(nodeId) {
    // Move to front of order
    editState.openCardOrder = editState.openCardOrder.filter(function(id) { return id !== nodeId; });
    editState.openCardOrder.unshift(nodeId);

    // Render first so DOM is rebuilt with correct order, then scroll
    renderCardStack();
    saveState();

    // Re-query after render since renderCardStack() replaces all DOM nodes
    document.querySelectorAll('.req-card').forEach(function(c) { c.classList.remove('focused'); });
    const card = document.getElementById('card-' + nodeId);
    if (card) {
        card.classList.add('focused');
        card.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }
}

function toggleCardCollapse(nodeId) {
    const info = editState.openCards.get(nodeId);
    if (!info) return;
    info.collapsed = !info.collapsed;
    const card = document.getElementById('card-' + nodeId);
    if (card) {
        card.classList.toggle('collapsed', info.collapsed);
    }
    saveState();
}

function closeAllCards() {
    editState.openCards.clear();
    editState.openCardOrder = [];
    renderCardStack();
    updateNavSelection();
    saveState();
}

function renderCardStack() {
    const body = document.getElementById('card-stack-body');
    const empty = document.getElementById('card-stack-empty');
    const countEl = document.getElementById('card-stack-count');

    if (editState.openCardOrder.length === 0) {
        body.innerHTML = '';
        body.appendChild(empty);
        empty.style.display = 'flex';
        countEl.textContent = 'No cards open';
        return;
    }

    empty.style.display = 'none';
    countEl.textContent = editState.openCardOrder.length + ' card' +
        (editState.openCardOrder.length !== 1 ? 's' : '') + ' open';

    let html = '';
    editState.openCardOrder.forEach(function(nodeId) {
        const info = editState.openCards.get(nodeId);
        if (!info) return;
        var kind = info.kind || 'requirement';
        if (kind === 'journey') {
            html += buildJourneyCardHtml(nodeId, info.data, info.collapsed);
        } else if (kind === 'requirement') {
            html += buildCardHtml(nodeId, info.data, info.collapsed);
        } else {
            html += buildGenericCardHtml(nodeId, info.data, info.collapsed);
        }
    });

    body.innerHTML = html;

    // Re-add empty element (hidden) for later
    body.appendChild(empty);

    // Re-apply cached coverage colors after DOM rebuild
    recolorCachedButtons();

    // Re-apply edit mode state after DOM rebuild
    if (editState.enabled) {
        document.querySelectorAll('.card-assertion-text').forEach(function(el) {
            el.contentEditable = 'true';
        });
    }
}

function buildCardHtml(nodeId, data, collapsed) {
    const collapsedClass = collapsed ? ' collapsed' : '';
    const focusedClass = editState.openCardOrder[0] === nodeId ? ' focused' : '';
    const shortId = nodeId.replace('REQ-', '');
    const title = escapeHtml(data.title || '');
    var props = data.properties || {};
    const level = (props.level || '').toUpperCase();
    const status = (props.status || '').toLowerCase();
    const statusDisplay = status.charAt(0).toUpperCase() + status.slice(1);
    const hash = props.hash || '';
    const sourcePath = (data.source && data.source.path) || '';
    const sourceLine = (data.source && data.source.line) || 0;

    let html = '<div class="req-card' + collapsedClass + focusedClass + '" id="card-' + nodeId + '">';

    // Header
    html += '<div class="req-card-header" onclick="toggleCardCollapse(\'' + nodeId + '\')">';
    html += '<span class="req-card-expand">&#9660;</span>';
    html += '<span class="req-card-id">' + shortId + '</span>';
    html += '<span class="req-card-title-display">' + title + '</span>';
    html += '<button class="req-card-delete edit-only" ' +
        'onclick="event.stopPropagation(); onDeleteRequirement(\'' + nodeId + '\')" ' +
        'title="Delete requirement">&#10005;</button>';
    html += '<button class="req-card-close" onclick="event.stopPropagation(); closeCard(\'' + nodeId + '\')" title="Close card">&times;</button>';
    html += '</div>';

    // Body
    html += '<div class="req-card-body">';

    // Coverage dimension badges (above metadata)
    // Only shown when data exists; always clickable to expand links + flash assertions
    if (data.coverage_dimensions) {
        var rLinks = data.req_level_links || {};
        var aLinksAll = data.assertion_links || {};
        html += '<div class="card-section card-coverage-badges" style="padding:4px 12px 0;display:flex;gap:6px;flex-wrap:wrap;">';
        var headerDims = [
            {key: 'implemented', linkKey: 'implemented', assertLinkKey: 'implemented', label: 'IMPLEMENTED'},
            {key: null, linkKey: 'refined', assertLinkKey: 'refined', label: 'REFINED'},
            {key: 'tested', linkKey: 'tested', assertLinkKey: 'tested', label: 'TESTED'},
            {key: 'verified', linkKey: 'verified', assertLinkKey: 'tested', label: 'VERIFIED'},
            {key: 'uat_coverage', linkKey: 'validated', assertLinkKey: 'validated', label: 'VALIDATED'},
            {key: 'uat_verified', linkKey: 'accepted', assertLinkKey: 'validated', label: 'ACCEPTED'}
        ];
        headerDims.forEach(function(d) {
            var dim = d.key ? data.coverage_dimensions[d.key] : null;
            var hasReqLinks = d.linkKey && rLinks[d.linkKey] && rLinks[d.linkKey].length > 0;
            // Check if any assertion has direct links for this dimension
            var hasAssertionLinks = false;
            for (var lbl in aLinksAll) {
                if (aLinksAll[lbl][d.assertLinkKey]) { hasAssertionLinks = true; break; }
            }
            var hasData = (dim && dim.color) || hasReqLinks || hasAssertionLinks;
            if (!hasData) return; // hide badge if no data
            var colorClass = dim && dim.color ? ' active val-' + dim.color : '';
            var tipParts = [];
            if (dim && dim.status_tip) tipParts.push(dim.status_tip);
            if (hasReqLinks) tipParts.push(rLinks[d.linkKey].length + ' REQ-level link(s)');
            if (hasAssertionLinks) tipParts.push('Click to highlight assertions');
            var tip = tipParts.length > 0 ? tipParts.join(' \u2014 ') : d.label;
            html += '<span class="status-badge' + colorClass + ' card-dim-badge" ' +
                'title="' + escapeHtml(tip) + '" style="font-size:0.6rem;padding:1px 5px;" ' +
                'onclick="event.stopPropagation(); toggleReqLevelLinks(this, \'' + nodeId + '\', \'' + d.linkKey + '\', \'' + d.assertLinkKey + '\')">' +
                d.label + '</span>';
        });
        html += '</div>';
        // Expandable link panels (one per header dimension, hidden by default)
        ['implemented', 'refined', 'tested', 'verified', 'validated', 'accepted'].forEach(function(dimKey) {
            html += '<div class="req-level-links-panel" id="req-links-' + nodeId + '-' + dimKey + '" style="display:none;margin:0 12px;padding:6px 10px;background:var(--assertion-code-panel-bg);border-radius:4px;border-left:3px solid var(--impl-yes-border);font-size:0.78rem;"></div>';
        });
    }

    // Metadata section
    html += '<div class="card-section"><div class="card-section-label">Metadata</div>';
    html += '<div class="card-meta">';

    // Level
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Level:</span>';
    html += '<span class="card-meta-value">' + level + '</span>';
    html += '</div>';

    // Status (badge or select)
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Status:</span>';
    html += '<span class="status-badge-wrapper"><span class="status-badge ' + status + '">' + statusDisplay + '</span></span>';
    html += '<span class="status-select-wrapper edit-only">';
    html += '<select class="status-select" data-node-id="' + nodeId + '" data-original="' + status + '" onchange="onStatusChange(this)">';
    ['Active', 'Draft', 'Deprecated', 'Proposed'].forEach(function(s) {
        const sel = s.toLowerCase() === status ? ' selected' : '';
        html += '<option value="' + s + '"' + sel + '>' + s + '</option>';
    });
    html += '</select></span>';
    html += '</div>';

    // Hash
    html += '<div class="card-meta-item">';
    html += '<span class="card-meta-key">Hash:</span>';
    html += '<span class="card-meta-value" style="font-family:monospace;font-size:0.75rem;">' + hash + '</span>';
    html += '</div>';

    // Source
    if (sourcePath) {
        html += '<div class="card-meta-item">';
        html += '<span class="card-meta-key">Source:</span>';
        html += '<a class="card-meta-value" href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeHtml(sourcePath) + '\', ' + sourceLine + ')" ' +
            'style="color:#0d6efd;cursor:pointer;font-size:0.75rem;text-decoration:none;">' +
            escapeHtml(sourcePath.split('/').pop()) + ':' + sourceLine + '</a>';
        html += ' <button class="card-move-file-btn edit-only" ' +
            'onclick="event.preventDefault(); onMoveToFile(\'' + nodeId + '\', \'' + escapeHtml(sourcePath) + '\')" ' +
            'title="Move to different file">Move</button>';
        html += '</div>';
    }

    html += '</div></div>';

    // Editable Title (edit mode only, replaces header title on blur)
    html += '<div class="card-section edit-only"><div class="card-section-label">Title <span class="edit-pencil" onclick="event.stopPropagation(); this.closest(\'.card-section\').querySelector(\'.editable-field\').focus()">&#9998;</span></div>';
    html += '<div class="editable-field" contenteditable="true" data-node-id="' + nodeId + '" ' +
        'data-field="title" data-original="' + escapeAttr(data.title || '') + '" ' +
        'onblur="onTitleBlur(this)" onkeydown="onEditableKeydown(event)">' +
        escapeHtml(data.title || '') + '</div>';
    html += '</div>';

    // Parents section (relationship editor in edit mode)
    html += '<div class="card-section"><div class="card-section-label">Implements / Refines / Satisfies</div>';
    html += '<div class="card-parents">';
    if (data.parents && data.parents.length > 0) {
        data.parents.forEach(function(p) {
            const edgeKind = (p.edge_kind || 'implements').toLowerCase();
            const edgeKindDisplay = edgeKind.charAt(0).toUpperCase() + edgeKind.slice(1);
            html += '<div class="card-parent-row">';
            // View-only kind badge
            html += '<span class="card-parent-kind view-only">' + edgeKindDisplay.toUpperCase() + '</span>';
            // Edit-only kind toggle
            html += '<button class="card-parent-kind-toggle edit-only" ' +
                'data-source="' + nodeId + '" data-target="' + p.id + '" data-kind="' + edgeKind + '" ' +
                'onclick="event.preventDefault(); onToggleEdgeKind(this)" ' +
                'title="Click to cycle edge kind">' + edgeKindDisplay + '</button>';
            html += '<a class="card-parent-link" href="#" onclick="event.preventDefault(); openCard(\'' + p.id + '\')">';
            html += (p.ref_id || p.id).replace('REQ-', '') + ' - ' + escapeHtml(p.title || '');
            html += '</a>';
            // Assertion targets display
            if (p.assertion_targets && p.assertion_targets.length > 0) {
                html += '<span class="card-parent-targets">[' + p.assertion_targets.join(', ') + ']</span>';
            }
            // Assertion targets picker button (edit mode, implements/refines only)
            if (edgeKind === 'implements' || edgeKind === 'refines') {
                html += '<button class="card-parent-targets-btn edit-only" ' +
                    'onclick="event.preventDefault(); editEdgeTargets(\'' + nodeId + '\', \'' + p.id + '\', this)" ' +
                    'title="Edit assertion targets">&#8943;</button>';
            }
            html += '<button class="card-parent-delete edit-only" ' +
                'onclick="event.preventDefault(); onDeleteEdge(\'' + nodeId + '\', \'' + p.id + '\')" ' +
                'title="Remove relationship">&times;</button>';
            html += '</div>';
        });
    } else {
        html += '<div class="card-parents-empty">No parent relationships</div>';
    }
    html += '</div>';
    // Add relationship button (edit mode only)
    html += '<button class="card-add-rel-btn edit-only" ' +
        'onclick="onAddRelationship(\'' + nodeId + '\', this)">+ Add Relationship</button>';
    html += '</div>';

    // Assertions section
    const assertions = (data.children || []).filter(function(c) { return c.kind === 'assertion'; });
    if (assertions.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Assertions (' + assertions.length + ')</div>';
        html += '<div class="card-assertions">';
        assertions.forEach(function(a) {
            const assertionId = a.id || '';
            const label = a.label || '';
            const text = a.text || '';
            html += '<div class="card-assertion-wrapper" data-assertion-id="' + assertionId + '">';
            html += '<div class="card-assertion">';
            html += '<span class="card-assertion-label">' + label + '</span>';
            html += '<span class="edit-pencil edit-only" onclick="event.stopPropagation(); this.closest(\'.card-assertion\').querySelector(\'.card-assertion-text\').focus()">&#9998;</span>';
            html += '<span class="card-assertion-text editable-field" ' +
                'contenteditable="false" data-assertion-id="' + assertionId + '" ' +
                'data-field="assertion" data-original="' + escapeAttr(text) + '" ' +
                'onblur="onAssertionBlur(this)" onkeydown="onEditableKeydown(event)">' +
                escapeHtml(text) + '</span>';
            // 6 dimension badges — only shown when direct assertion-level links exist
            html += '<span class="assertion-badges-area">';
            var badgeMode = editState.assertionBadgeMode || 'abbrev';
            var aLinks = data.assertion_links ? data.assertion_links[label] : null;
            var dimDefs = [
                {cls: 'assertion-implemented-btn', full: 'Implemented', abbr: 'IMP', dot: 'I', toggle: 'toggleAssertionCode', tip: 'Code implementation', linkKey: 'implemented'},
                {cls: 'assertion-refined-btn', full: 'Refined', abbr: 'REF', dot: 'R', toggle: 'toggleAssertionCode', tip: 'Refines references', linkKey: 'refined'},
                {cls: 'assertion-tested-btn', full: 'Tested', abbr: 'TST', dot: 'T', toggle: 'toggleAssertionTests', tip: 'Test coverage', linkKey: 'tested'},
                {cls: 'assertion-verified-btn', full: 'Verified', abbr: 'VER', dot: 'V', toggle: 'toggleAssertionResults', tip: 'Test results', linkKey: 'tested'},
                {cls: 'assertion-validated-btn', full: 'Validated', abbr: 'VAL', dot: 'U', toggle: 'toggleAssertionUat', tip: 'Journey coverage', linkKey: 'validated'},
                {cls: 'assertion-accepted-btn', full: 'Accepted', abbr: 'ACC', dot: 'A', toggle: 'toggleAssertionUatResults', tip: 'Journey results', linkKey: 'validated'}
            ];
            var anyBadge = false;
            dimDefs.forEach(function(dim) {
                // Only show badge if direct assertion-level links exist for this dimension
                if (aLinks && !aLinks[dim.linkKey]) return;
                anyBadge = true;
                var btnLabel = badgeMode === 'full' ? dim.full : (badgeMode === 'dots' ? dim.dot : dim.abbr);
                var btnStyle = badgeMode === 'dots' ? ' style="min-width:0;padding:2px 4px;font-size:0.55rem;"' : '';
                html += '<button class="' + dim.cls + '" ' +
                    'data-req-id="' + nodeId + '" data-label="' + escapeAttr(label) + '" ' +
                    'title="' + dim.tip + '" ' +
                    'onclick="event.stopPropagation(); ' + dim.toggle + '(this, \'' + nodeId + '\', \'' + escapeAttr(label) + '\')"' +
                    btnStyle + '>' +
                    btnLabel + '</button>';
            });
            if (!anyBadge) {
                html += '<span class="assertion-no-coverage" title="No direct assertion-level coverage" style="font-size:0.6rem;color:var(--secondary-text);opacity:0.5;">no direct coverage</span>';
            }
            html += '</span>'; // close .assertion-badges-area
            html += '<button class="assertion-delete-btn edit-only" ' +
                'title="Deprecate assertion" ' +
                'onclick="event.stopPropagation(); onDeleteAssertion(\'' + nodeId + '\', \'' + escapeAttr(assertionId) + '\')">' +
                '&#8856;</button>';
            html += '</div>';
            html += '<div class="assertion-code-panel" id="assertion-code-' + nodeId + '-' + label + '" style="display:none;"></div>';
            html += '<div class="assertion-tests-panel" id="assertion-tests-' + nodeId + '-' + label + '" style="display:none;"></div>';
            html += '<div class="assertion-results-panel" id="assertion-results-' + nodeId + '-' + label + '" style="display:none;"></div>';
            html += '<div class="assertion-uat-panel" id="assertion-uat-' + nodeId + '-' + label + '" style="display:none;"></div>';
            html += '<div class="assertion-uat-results-panel" id="assertion-uat-results-' + nodeId + '-' + label + '" style="display:none;"></div>';
            html += '</div>';
        });
        html += '</div>';

        // Add assertion button (edit mode only)
        html += '<button class="card-add-assertion-btn edit-only" onclick="onAddAssertion(\'' + nodeId + '\')">+ Add Assertion</button>';
        html += '</div>';
    } else {
        html += '<div class="card-section"><div class="card-section-label">Assertions</div>';
        html += '<div style="font-size:0.8rem;color:#adb5bd;">No assertions defined</div>';
        html += '<button class="card-add-assertion-btn edit-only" onclick="onAddAssertion(\'' + nodeId + '\')">+ Add Assertion</button>';
        html += '</div>';
    }

    // Children (requirements only)
    const childReqs = (data.children || []).filter(function(c) {
        return c.kind === 'requirement';
    });
    if (childReqs.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Children (' + childReqs.length + ')</div>';
        html += '<div class="card-children">';
        childReqs.forEach(function(c) {
            html += '<a class="card-child-link" href="#" onclick="event.preventDefault(); openCard(\'' + c.id + '\')">';
            html += c.id.replace('REQ-', '');
            if (c.title) html += ' - ' + escapeHtml(c.title);
            html += '</a>';
        });
        html += '</div></div>';
    }

    html += '</div>'; // end req-card-body
    html += '</div>'; // end req-card

    return html;
}

function updateNavSelection() {
    document.querySelectorAll('.nav-tree-row').forEach(function(row) {
        row.classList.toggle('selected', editState.openCards.has(row.dataset.id));
    });
}

// =====================================================================
// Journey Cards — reads from generic /api/node/ response
// =====================================================================

function buildJourneyCardHtml(nodeId, data, collapsed) {
    var collapsedClass = collapsed ? ' collapsed' : '';
    var focusedClass = editState.openCardOrder[0] === nodeId ? ' focused' : '';
    var title = escapeHtml(data.title || '');
    var shortId = escapeHtml(data.id || nodeId);
    var props = data.properties || {};
    var sourcePath = (data.source && data.source.path) || '';
    var sourceLine = (data.source && data.source.line) || 1;

    var html = '<div class="req-card journey-card' + collapsedClass + focusedClass + '" id="card-' + nodeId + '">';

    // Header
    html += '<div class="req-card-header" onclick="toggleCardCollapse(\'' + nodeId + '\')">';
    html += '<span class="req-card-expand">&#9660;</span>';
    html += '<span class="journey-card-kind">JNY</span>';
    html += '<span class="req-card-title-display">' + title + '</span>';
    html += '<button class="req-card-close" onclick="event.stopPropagation(); closeCard(\'' + nodeId + '\')" title="Close card">&times;</button>';
    html += '</div>';

    // Body
    html += '<div class="req-card-body">';

    // Metadata section
    html += '<div class="card-section"><div class="card-section-label">Journey Metadata</div>';
    html += '<div class="card-meta">';

    if (props.actor) {
        html += '<div class="card-meta-item">';
        html += '<span class="card-meta-key">Actor:</span>';
        html += '<span class="card-meta-value">' + escapeHtml(props.actor) + '</span>';
        html += '</div>';
    }

    if (props.goal) {
        html += '<div class="card-meta-item">';
        html += '<span class="card-meta-key">Goal:</span>';
        html += '<span class="card-meta-value">' + escapeHtml(props.goal) + '</span>';
        html += '</div>';
    }

    if (props.descriptor) {
        html += '<div class="card-meta-item">';
        html += '<span class="card-meta-key">Topic:</span>';
        html += '<span class="card-meta-value">' + escapeHtml(props.descriptor) + '</span>';
        html += '</div>';
    }

    if (sourcePath) {
        html += '<div class="card-meta-item">';
        html += '<span class="card-meta-key">Source:</span>';
        html += '<a class="card-meta-value" href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeHtml(sourcePath) + '\', ' + sourceLine + ')" ' +
            'style="color:#0d6efd;cursor:pointer;font-size:0.75rem;text-decoration:none;">' +
            escapeHtml(sourcePath.split('/').pop()) + '</a>';
        html += '</div>';
    }

    html += '</div></div>';

    // Referenced Requirements (from links array — ADDRESSES edges)
    var refs = (data.links || []).filter(function(l) { return l.edge_kind === 'validates'; });
    if (refs.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Addresses (' + refs.length + ')</div>';
        html += '<div class="card-parents">';
        refs.forEach(function(link) {
            html += '<a class="card-parent-link" href="#" onclick="event.preventDefault(); openCard(\'' + link.id + '\')">';
            html += link.id.replace('REQ-', '');
            if (link.title) html += ' - ' + escapeHtml(link.title);
            html += '</a>';
        });
        html += '</div></div>';
    }

    // Journey body (full text with steps)
    if (props.description) {
        html += '<div class="card-section"><div class="card-section-label">Journey Body</div>';
        html += '<div class="journey-card-description">' + simpleMarkdown(props.description) + '</div>';
        html += '</div>';
    }

    html += '</div>'; // end req-card-body
    html += '</div>'; // end req-card

    return html;
}

// =====================================================================
// Generic Card — fallback for TEST, CODE, RESULT, etc.
// =====================================================================

function buildGenericCardHtml(nodeId, data, collapsed) {
    var collapsedClass = collapsed ? ' collapsed' : '';
    var focusedClass = editState.openCardOrder[0] === nodeId ? ' focused' : '';
    var title = escapeHtml(data.title || nodeId);
    var kind = data.kind || 'unknown';
    var kindLabel = kind.charAt(0).toUpperCase() + kind.slice(1);
    var props = data.properties || {};
    var sourcePath = (data.source && data.source.path) || '';
    var sourceLine = (data.source && data.source.line) || 0;

    var html = '<div class="req-card' + collapsedClass + focusedClass + '" id="card-' + nodeId + '">';

    // Header
    html += '<div class="req-card-header" onclick="toggleCardCollapse(\'' + nodeId + '\')">';
    html += '<span class="req-card-expand">&#9660;</span>';
    html += '<span class="journey-card-kind">' + escapeHtml(kindLabel) + '</span>';
    html += '<span class="req-card-title-display">' + title + '</span>';
    html += '<button class="req-card-close" onclick="event.stopPropagation(); closeCard(\'' + nodeId + '\')" title="Close card">&times;</button>';
    html += '</div>';

    // Body
    html += '<div class="req-card-body">';

    // Source
    if (sourcePath) {
        html += '<div class="card-section"><div class="card-section-label">Source</div>';
        html += '<div class="card-meta"><div class="card-meta-item">';
        html += '<a class="card-meta-value" href="#" onclick="event.preventDefault(); showSource(\'' +
            escapeHtml(sourcePath) + '\', ' + sourceLine + ')" ' +
            'style="color:#0d6efd;cursor:pointer;font-size:0.75rem;text-decoration:none;">' +
            escapeHtml(sourcePath.split('/').pop()) + ':' + sourceLine + '</a>';
        html += '</div></div></div>';
    }

    // Properties as key-value pairs
    var propKeys = Object.keys(props);
    if (propKeys.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Properties</div>';
        html += '<div class="card-meta">';
        propKeys.forEach(function(key) {
            var val = props[key];
            if (val === null || val === undefined || val === '') return;
            html += '<div class="card-meta-item">';
            html += '<span class="card-meta-key">' + escapeHtml(key) + ':</span>';
            html += '<span class="card-meta-value">' + escapeHtml(String(val)) + '</span>';
            html += '</div>';
        });
        html += '</div></div>';
    }

    // Links
    var links = data.links || [];
    if (links.length > 0) {
        html += '<div class="card-section"><div class="card-section-label">Links (' + links.length + ')</div>';
        html += '<div class="card-parents">';
        links.forEach(function(link) {
            html += '<a class="card-parent-link" href="#" onclick="event.preventDefault(); openCard(\'' + link.id + '\')">';
            html += '<span class="card-parent-kind">' + escapeHtml(link.edge_kind || '') + '</span> ';
            html += escapeHtml(link.id);
            if (link.title) html += ' - ' + escapeHtml(link.title);
            html += '</a>';
        });
        html += '</div></div>';
    }

    html += '</div>'; // end req-card-body
    html += '</div>'; // end req-card

    return html;
}

// =====================================================================
// Simple Markdown Renderer (for journey body text)
// =====================================================================

function simpleMarkdown(text) {
    // Escape HTML first, then add minimal formatting
    var lines = text.split('\n');
    var result = [];
    for (var i = 0; i < lines.length; i++) {
        var line = escapeHtml(lines[i]);
        // Bold: **text**
        line = line.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
        // Headers: ## text -> bold
        line = line.replace(/^(#{1,3})\s+(.+)$/, '<strong>$2</strong>');
        // Numbered steps: 1. text
        line = line.replace(/^(\d+)\.\s+/, '<strong>$1.</strong> ');
        // Bullet items: - text
        line = line.replace(/^(\s*)-\s+/, '$1&bull; ');
        result.push(line);
    }
    return result.join('\n');
}

// =====================================================================
// Coverage Prefetch — color buttons on card open
// =====================================================================

async function prefetchCoverage(nodeId) {
    var testData = assertionTestsCache[nodeId]
        ? Promise.resolve(assertionTestsCache[nodeId])
        : apiFetch('/api/test-coverage/' + encodeURIComponent(nodeId));
    var codeData = assertionCodeCache[nodeId]
        ? Promise.resolve(assertionCodeCache[nodeId])
        : apiFetch('/api/code-coverage/' + encodeURIComponent(nodeId));
    var uatData = assertionUatCache[nodeId]
        ? Promise.resolve(assertionUatCache[nodeId])
        : apiFetch('/api/uat-coverage/' + encodeURIComponent(nodeId));

    var results = await Promise.all([testData, codeData, uatData]);

    if (results[0] && results[0].success) {
        assertionTestsCache[nodeId] = results[0];
    }
    if (results[1] && results[1].success) {
        assertionCodeCache[nodeId] = results[1];
    }
    if (results[2] && results[2].success) {
        assertionUatCache[nodeId] = results[2];
    }
    recolorCachedButtons();
}

function recolorCachedButtons() {
    for (var reqId in assertionTestsCache) {
        updateAllTestedButtons(reqId, assertionTestsCache[reqId]);
        updateAllVerifiedButtons(reqId, assertionTestsCache[reqId]);
    }
    for (var reqId in assertionCodeCache) {
        updateAllImplementedButtons(reqId, assertionCodeCache[reqId]);
    }
    for (var reqId in assertionUatCache) {
        updateAllValidatedButtons(reqId, assertionUatCache[reqId]);
        updateAllAcceptedButtons(reqId, assertionUatCache[reqId]);
    }
}

function setAssertionBadgeMode(mode) {
    editState.assertionBadgeMode = mode;
    renderCardStack();
    recolorCachedButtons();
}

// =====================================================================
// Assertion Validation — Test Coverage Buttons
// =====================================================================

var assertionTestsCache = {};

async function toggleAssertionTests(btn, reqId, label) {
    var panelId = 'assertion-tests-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    // Toggle visibility
    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    // Fetch data if not cached
    if (!assertionTestsCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/test-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load coverage data</div>';
            return;
        }
        assertionTestsCache[reqId] = data;
        // Update ALL tested/verified buttons for this requirement
        updateAllTestedButtons(reqId, data);
        updateAllVerifiedButtons(reqId, data);
    }

    renderAssertionTests(panel, reqId, label, assertionTestsCache[reqId]);
}

function renderAssertionTests(panel, reqId, label, data) {
    var assertionData = data.assertion_tests[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var tests = assertionData.tests;
    if (tests.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No tests cover this assertion</div>';
        return;
    }

    var html = '';
    for (var i = 0; i < tests.length; i++) {
        var t = tests[i];
        var statusIcon = '';
        var statusClass = '';
        // Determine overall status from results
        var hasResults = t.results && t.results.length > 0;
        if (hasResults) {
            var allPassed = t.results.every(function(r) { return r.status === 'passed'; });
            var anyFailed = t.results.some(function(r) { return r.status === 'failed' || r.status === 'failure' || r.status === 'error'; });
            if (allPassed) { statusIcon = '&#x2705;'; statusClass = 'test-passed'; }
            else if (anyFailed) { statusIcon = '&#x274C;'; statusClass = 'test-failed'; }
            else { statusIcon = '&#x2753;'; statusClass = ''; }
        } else {
            statusIcon = '&#x2796;'; statusClass = 'test-no-result';
        }

        var testLabel = escapeHtml(t.label || t.id);

        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        if (t.file) {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeHtml(t.file) + '\', ' + (t.line || 0) + ')">' + testLabel + '</a>';
        } else {
            html += '<span class="assertion-test-link">' + testLabel + '</span>';
        }
        // Result file link (e.g., test-results.xml)
        if (hasResults && t.results[0].file) {
            html += ' <a class="assertion-result-file-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeHtml(t.results[0].file) + '\', ' + (t.results[0].line || 0) +
                ')" title="View result in ' + escapeHtml(t.results[0].file) + '">[result]</a>';
        }
        html += '</div>';
    }
    panel.innerHTML = html;
}

// =====================================================================
// Assertion Results — Test Result Files (VER badge)
// =====================================================================

async function toggleAssertionResults(btn, reqId, label) {
    var panelId = 'assertion-results-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    // Fetch test data if not cached (results come from the same endpoint)
    if (!assertionTestsCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/test-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load results data</div>';
            return;
        }
        assertionTestsCache[reqId] = data;
        updateAllTestedButtons(reqId, data);
        updateAllVerifiedButtons(reqId, data);
    }

    renderAssertionResults(panel, reqId, label, assertionTestsCache[reqId]);
}

function renderAssertionResults(panel, reqId, label, data) {
    var assertionData = data.assertion_tests[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    // Collect all results across all tests
    var resultItems = [];
    var tests = assertionData.tests;
    for (var i = 0; i < tests.length; i++) {
        var t = tests[i];
        if (t.results && t.results.length > 0) {
            for (var j = 0; j < t.results.length; j++) {
                resultItems.push({ result: t.results[j], testLabel: t.label || t.id });
            }
        }
    }

    if (resultItems.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No test results for this assertion</div>';
        return;
    }

    var html = '';
    for (var k = 0; k < resultItems.length; k++) {
        var r = resultItems[k].result;
        var tLabel = resultItems[k].testLabel;
        var status = (r.status || '').toLowerCase();
        var statusIcon = '';
        var statusClass = '';
        if (status === 'passed' || status === 'pass' || status === 'success') {
            statusIcon = '&#x2705;'; statusClass = 'test-passed';
        } else if (status === 'failed' || status === 'fail' || status === 'failure' || status === 'error') {
            statusIcon = '&#x274C;'; statusClass = 'test-failed';
        } else {
            statusIcon = '&#x2753;'; statusClass = '';
        }

        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        if (r.file) {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeHtml(r.file) + '\', ' + (r.line || 0) + ')">' +
                escapeHtml(tLabel) + ' [' + escapeHtml(r.status) + ']</a>';
        } else {
            html += '<span class="assertion-test-link">' + escapeHtml(tLabel) + ' [' + escapeHtml(r.status) + ']</span>';
        }
        html += '</div>';
    }
    panel.innerHTML = html;
}

// Shared helper: classify test/journey coverage into a state class
function _classifyTestState(items) {
    // items = array of test/journey objects with .results arrays
    // Returns: 'validation-pass', 'validation-partial', 'validation-fail', or 'validation-unknown'
    if (items.length === 0) return 'validation-fail';
    var allResults = [];
    for (var j = 0; j < items.length; j++) {
        if (items[j].results) {
            for (var k = 0; k < items[j].results.length; k++) {
                allResults.push(items[j].results[k].status);
            }
        }
    }
    if (allResults.length === 0) return 'validation-unknown'; // exists but no results = unknown
    var allPassed = allResults.every(function(s) { return s === 'passed'; });
    var anyFailed = allResults.some(function(s) { return s === 'failed' || s === 'failure' || s === 'error'; });
    if (allPassed) return 'validation-pass';
    if (anyFailed) return 'validation-partial';
    return 'validation-pass'; // unknown statuses (skipped) = covered
}

function updateAllTestedButtons(reqId, data) {
    // TESTED: do tests exist for this assertion? (yes/no, same CSS as implemented)
    var buttons = document.querySelectorAll('.assertion-tested-btn[data-req-id="' + reqId + '"]');
    for (var i = 0; i < buttons.length; i++) {
        var btn = buttons[i];
        var label = btn.dataset.label;
        var assertionData = data.assertion_tests[label];
        if (!assertionData) continue;
        btn.classList.remove('implemented-yes', 'implemented-no');
        btn.classList.add(assertionData.tests.length > 0 ? 'implemented-yes' : 'implemented-no');
    }
}

function updateAllVerifiedButtons(reqId, data) {
    // VERIFIED: do test results pass? (pass/partial/fail)
    var buttons = document.querySelectorAll('.assertion-verified-btn[data-req-id="' + reqId + '"]');
    for (var i = 0; i < buttons.length; i++) {
        var btn = buttons[i];
        var label = btn.dataset.label;
        var assertionData = data.assertion_tests[label];
        if (!assertionData) continue;
        btn.classList.remove('validation-pass', 'validation-partial', 'validation-fail', 'validation-unknown');
        btn.classList.add(_classifyTestState(assertionData.tests));
    }
}

// =====================================================================
// Assertion Implementation — Code Coverage Buttons
// =====================================================================

var assertionCodeCache = {};

async function toggleAssertionCode(btn, reqId, label) {
    var panelId = 'assertion-code-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    // Toggle visibility
    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    // Fetch data if not cached
    if (!assertionCodeCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/code-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load implementation data</div>';
            return;
        }
        assertionCodeCache[reqId] = data;
        // Update ALL implemented buttons for this requirement
        updateAllImplementedButtons(reqId, data);
    }

    renderAssertionCode(panel, reqId, label, assertionCodeCache[reqId]);
}

function renderAssertionCode(panel, reqId, label, data) {
    var assertionData = data.assertion_code[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var codeRefs = assertionData.code_refs;
    if (codeRefs.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No code implements this assertion</div>';
        return;
    }

    var html = '';
    for (var i = 0; i < codeRefs.length; i++) {
        var c = codeRefs[i];
        // Extract short filename from full path
        var fileName = c.file ? c.file.split('/').pop() : c.id;

        html += '<div class="assertion-test-item">';
        html += '<span class="assertion-test-icon">&#x1F4C4;</span>';
        if (c.file) {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeHtml(c.file) + '\', ' + (c.line || 0) + ')">' +
                escapeHtml(fileName) + ':' + (c.line || 1) + '</a>';
        } else {
            html += '<span class="assertion-test-link">' + escapeHtml(c.label || c.id) + '</span>';
        }
        html += '</div>';
    }
    panel.innerHTML = html;
}

function updateAllImplementedButtons(reqId, data) {
    var buttons = document.querySelectorAll('.assertion-implemented-btn[data-req-id="' + reqId + '"]');
    for (var i = 0; i < buttons.length; i++) {
        var btn = buttons[i];
        var label = btn.dataset.label;
        var assertionData = data.assertion_code[label];
        if (!assertionData) continue;

        // Remove previous state classes
        btn.classList.remove('implemented-yes', 'implemented-no');

        var codeRefs = assertionData.code_refs;
        if (codeRefs.length > 0) {
            btn.classList.add('implemented-yes');
        } else {
            btn.classList.add('implemented-no');
        }
    }
}

// =====================================================================
// Assertion UAT — Journey Coverage Buttons (Validated + Accepted)
// =====================================================================

var assertionUatCache = {};

async function toggleAssertionUat(btn, reqId, label) {
    var panelId = 'assertion-uat-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    // Toggle visibility
    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    // Fetch data if not cached
    if (!assertionUatCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/uat-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load UAT data</div>';
            return;
        }
        assertionUatCache[reqId] = data;
        updateAllValidatedButtons(reqId, data);
        updateAllAcceptedButtons(reqId, data);
    }

    renderAssertionUat(panel, reqId, label, assertionUatCache[reqId]);
}

function renderAssertionUat(panel, reqId, label, data) {
    var assertionData = data.assertion_journeys[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var journeys = assertionData.journeys;
    if (journeys.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No journeys validate this assertion</div>';
        return;
    }

    // Reuse the same rendering as tests (journeys have same structure)
    var html = '';
    for (var i = 0; i < journeys.length; i++) {
        var j = journeys[i];
        var statusIcon = '';
        var statusClass = '';
        var hasResults = j.results && j.results.length > 0;
        if (hasResults) {
            var allPassed = j.results.every(function(r) { return r.status === 'passed'; });
            var anyFailed = j.results.some(function(r) { return r.status === 'failed' || r.status === 'failure' || r.status === 'error'; });
            if (allPassed) { statusIcon = '&#x2705;'; statusClass = 'test-passed'; }
            else if (anyFailed) { statusIcon = '&#x274C;'; statusClass = 'test-failed'; }
            else { statusIcon = '&#x2753;'; statusClass = ''; }
        } else {
            statusIcon = '&#x2796;'; statusClass = 'test-no-result';
        }

        var jLabel = escapeHtml(j.label || j.id);
        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        // Journey links open the journey card instead of showing source
        html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
            escapeHtml(j.id) + '\')">' + jLabel + '</a>';
        html += '</div>';
    }
    panel.innerHTML = html;
}

function updateAllValidatedButtons(reqId, data) {
    // VALIDATED: do journeys exist for this assertion? (yes/no)
    var buttons = document.querySelectorAll('.assertion-validated-btn[data-req-id="' + reqId + '"]');
    for (var i = 0; i < buttons.length; i++) {
        var btn = buttons[i];
        var label = btn.dataset.label;
        var assertionData = data.assertion_journeys[label];
        if (!assertionData) continue;
        btn.classList.remove('implemented-yes', 'implemented-no');
        btn.classList.add(assertionData.journeys.length > 0 ? 'implemented-yes' : 'implemented-no');
    }
}

function updateAllAcceptedButtons(reqId, data) {
    // ACCEPTED: do journey results pass? (pass/partial/fail)
    var buttons = document.querySelectorAll('.assertion-accepted-btn[data-req-id="' + reqId + '"]');
    for (var i = 0; i < buttons.length; i++) {
        var btn = buttons[i];
        var label = btn.dataset.label;
        var assertionData = data.assertion_journeys[label];
        if (!assertionData) continue;
        btn.classList.remove('validation-pass', 'validation-partial', 'validation-fail', 'validation-unknown');
        btn.classList.add(_classifyTestState(assertionData.journeys));
    }
}

// =====================================================================
// Assertion UAT Results — Journey Result Files (ACC badge)
// =====================================================================

async function toggleAssertionUatResults(btn, reqId, label) {
    var panelId = 'assertion-uat-results-' + reqId + '-' + label;
    var panel = document.getElementById(panelId);
    if (!panel) return;

    if (panel.style.display !== 'none') {
        panel.style.display = 'none';
        btn.classList.remove('active');
        return;
    }

    panel.style.display = 'block';
    btn.classList.add('active');

    if (!assertionUatCache[reqId]) {
        panel.innerHTML = '<div class="assertion-tests-loading">Loading...</div>';
        var data = await apiFetch('/api/uat-coverage/' + encodeURIComponent(reqId));
        if (!data || !data.success) {
            panel.innerHTML = '<div class="assertion-tests-loading">Failed to load UAT results</div>';
            return;
        }
        assertionUatCache[reqId] = data;
        updateAllValidatedButtons(reqId, data);
        updateAllAcceptedButtons(reqId, data);
    }

    renderAssertionUatResults(panel, reqId, label, assertionUatCache[reqId]);
}

function renderAssertionUatResults(panel, reqId, label, data) {
    var assertionData = data.assertion_journeys[label];
    if (!assertionData) {
        panel.innerHTML = '<div class="assertion-tests-empty">No data for this assertion</div>';
        return;
    }

    var resultItems = [];
    var journeys = assertionData.journeys;
    for (var i = 0; i < journeys.length; i++) {
        var j = journeys[i];
        if (j.results && j.results.length > 0) {
            for (var k = 0; k < j.results.length; k++) {
                resultItems.push({ result: j.results[k], jnyLabel: j.label || j.id, jnyId: j.id });
            }
        }
    }

    if (resultItems.length === 0) {
        panel.innerHTML = '<div class="assertion-tests-empty">No journey results for this assertion</div>';
        return;
    }

    var html = '';
    for (var r = 0; r < resultItems.length; r++) {
        var res = resultItems[r].result;
        var jLabel = resultItems[r].jnyLabel;
        var status = (res.status || '').toLowerCase();
        var statusIcon = '', statusClass = '';
        if (status === 'passed' || status === 'pass' || status === 'success') {
            statusIcon = '&#x2705;'; statusClass = 'test-passed';
        } else if (status === 'failed' || status === 'fail' || status === 'failure' || status === 'error') {
            statusIcon = '&#x274C;'; statusClass = 'test-failed';
        } else {
            statusIcon = '&#x2753;';
        }

        html += '<div class="assertion-test-item ' + statusClass + '">';
        html += '<span class="assertion-test-icon">' + statusIcon + '</span>';
        if (res.file) {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                escapeHtml(res.file) + '\', ' + (res.line || 0) + ')">' +
                escapeHtml(jLabel) + ' [' + escapeHtml(res.status) + ']</a>';
        } else {
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                escapeHtml(resultItems[r].jnyId) + '\')">' +
                escapeHtml(jLabel) + ' [' + escapeHtml(res.status) + ']</a>';
        }
        html += '</div>';
    }
    panel.innerHTML = html;
}

// =====================================================================
// REQ-Level Link Expansion (header badge click)
// =====================================================================

function toggleReqLevelLinks(badge, reqId, dimKey, assertLinkKey) {
    var panelId = 'req-links-' + reqId + '-' + dimKey;
    var panel = document.getElementById(panelId);

    if (panel && panel.style.display !== 'none') {
        // Closing — just hide, no flash/scroll
        panel.style.display = 'none';
        return;
    }

    // Opening — flash assertions that have direct links
    _flashAssertions(reqId, assertLinkKey || dimKey);

    if (!panel) return;

    // Find the card data from editState
    var cardState = editState.openCards.get(reqId);
    if (!cardState || !cardState.data || !cardState.data.req_level_links) {
        return; // no panel to show, flash was enough
    }

    var links = cardState.data.req_level_links[dimKey] || [];
    if (links.length === 0) {
        return; // no REQ-level links, flash was enough
    }

    var html = '';
    for (var i = 0; i < links.length; i++) {
        var link = links[i];
        // Result status icon for TEST/JNY nodes
        var statusIcon = '';
        if (link.result_status === 'passed') statusIcon = '&#x2705; ';
        else if (link.result_status === 'failed') statusIcon = '&#x274C; ';
        else if (link.result_status === 'mixed') statusIcon = '&#x26A0; ';

        var statusClass = '';
        if (link.result_status === 'failed') statusClass = ' test-failed';
        else if (link.result_status === 'passed') statusClass = ' test-passed';

        if (link.kind === 'journey') {
            html += '<div class="assertion-test-item' + statusClass + '">';
            html += '<span class="assertion-test-icon">' + statusIcon + '&#x1F4D6;</span>';
            html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                escapeHtml(link.id) + '\')">' + escapeHtml(link.title) + '</a>';
            html += '</div>';
        } else {
            var icon = link.kind === 'requirement' ? '&#x1F4CB;' : (link.kind === 'test' ? '&#x1F9EA;' : '&#x1F4C4;');
            html += '<div class="assertion-test-item' + statusClass + '">';
            html += '<span class="assertion-test-icon">' + statusIcon + icon + '</span>';
            if (link.file) {
                html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); showSource(\'' +
                    escapeHtml(link.file) + '\', ' + (link.line || 0) + ')">' + escapeHtml(link.title) + '</a>';
            } else {
                html += '<a class="assertion-test-link" href="#" onclick="event.preventDefault(); openCard(\'' +
                    escapeHtml(link.id) + '\')">' + escapeHtml(link.title) + '</a>';
            }
            html += '</div>';
        }
    }
    panel.innerHTML = html;
    panel.style.display = 'block';
}

function _flashAssertions(reqId, assertLinkKey) {
    // Find card data to get assertion_links
    var cardState = editState.openCards.get(reqId);
    if (!cardState || !cardState.data || !cardState.data.assertion_links) return;

    var aLinks = cardState.data.assertion_links;
    var card = document.getElementById('card-' + reqId);
    if (!card) return;

    // Collect matching .card-assertion elements (they have the visible background)
    var targets = [];
    for (var label in aLinks) {
        if (!aLinks[label][assertLinkKey]) continue;
        var wrappers = card.querySelectorAll('.card-assertion-wrapper');
        for (var i = 0; i < wrappers.length; i++) {
            var assertionDiv = wrappers[i].querySelector('.card-assertion');
            if (!assertionDiv) continue;
            var labelEl = assertionDiv.querySelector('.card-assertion-label');
            if (labelEl && labelEl.textContent.trim() === label) {
                targets.push(assertionDiv);
            }
        }
    }

    if (targets.length > 0) {
        targets[0].scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }

    // Use inline style flash (same pattern as file-viewer line flash)
    setTimeout(function() {
        for (var j = 0; j < targets.length; j++) {
            targets[j].style.transition = 'none';
            targets[j].style.backgroundColor = 'rgba(13, 110, 253, 0.3)';
        }
        setTimeout(function() {
            for (var j = 0; j < targets.length; j++) {
                targets[j].style.transition = 'background-color 1s ease-out';
                targets[j].style.backgroundColor = '';
            }
        }, 400);
    }, 50);
}
