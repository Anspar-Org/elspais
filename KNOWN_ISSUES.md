# Known Issues

[x] Viewer Git Push (v0.100.0)
- option to create a branch / set branch name
- git push button
- warn if closing window with unsaved changes

[ ] Update the Legend
- Maybe also add a "More Help" mode, where the hover-help text shows in a fixed part of the header bar (so it doesn't obscure data being viewed), and is more descriptive.

[ ] Spec file naming convention
Rename spec/ files more sensibly  and consistently
Renumber REQs to follow a pre-fix-per-file convention (not enforced, just for convenience)
Maybe a tool to help with renumber REQs (it will have to check all code, test, doc, etc files and make approte replacements.
