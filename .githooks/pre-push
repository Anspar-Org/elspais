#!/bin/bash
# =====================================================
# Pre-Push Hook: elspais
# =====================================================
#
# Runs ONLY checks that pre-commit doesn't already cover:
#   1. Branch freshness (must be rebased on origin/main)
#   2. Version bump (auto-bumps if same as main)
#   3. E2E tests (cached)
#   4. Secret detection (gitleaks)
#   5. Documentation sync tests
#
# Pre-commit already handles: lint, format, markdown, unit tests,
# elspais fix, commit message format.
#
# PR-aware blocking:
#   - PR/feature branch: failures BLOCK the push
#   - Other branches: failures show warnings only
#
# To install: make setup
# To bypass (NOT RECOMMENDED): git push --no-verify
#
# =====================================================

set -e

# Clean git env for subprocess isolation
unset GIT_DIR
unset GIT_WORK_TREE

# Colors
RED='\033[0;31m'
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m'

CURRENT_BRANCH=$(git branch --show-current)

# Skip for main/master (only receives validated PRs)
if [ "$CURRENT_BRANCH" = "main" ] || [ "$CURRENT_BRANCH" = "master" ]; then
    echo -e "${BLUE}Skipping pre-push validation for protected branch '$CURRENT_BRANCH'${NC}"
    exit 0
fi

# =====================================================
# 1. Branch Freshness & Version Bump
# =====================================================

git fetch origin main --quiet 2>/dev/null || true

if git rev-parse origin/main >/dev/null 2>&1; then
    BEHIND_COUNT=$(git rev-list --count HEAD..origin/main)
    if [ "$BEHIND_COUNT" -gt 0 ]; then
        echo ""
        echo -e "${RED}======================================${NC}"
        echo -e "${RED}  Branch is $BEHIND_COUNT commit(s) behind origin/main${NC}"
        echo -e "${RED}======================================${NC}"
        echo ""
        echo "Please rebase before pushing:"
        echo "  git pull --rebase origin main"
        echo ""
        exit 1
    fi

    # Auto-bump version if same as main
    MAIN_VERSION=$(git show origin/main:pyproject.toml | grep '^version' | head -1 | sed 's/.*"\(.*\)"/\1/')
    BRANCH_VERSION=$(grep '^version' pyproject.toml | head -1 | sed 's/.*"\(.*\)"/\1/')

    if [ "$MAIN_VERSION" = "$BRANCH_VERSION" ]; then
        CHANGED_FILES=$(git diff --name-only origin/main...HEAD)

        PATCH_ONLY=true
        while IFS= read -r file; do
            [ -z "$file" ] && continue
            case "$file" in
                docs/*|tests/*|spec/*|*.md|.gitignore|.github/workflows/*)
                    ;; # patch-level
                *)
                    PATCH_ONLY=false
                    break
                    ;;
            esac
        done <<< "$CHANGED_FILES"

        IFS='.' read -r MAJOR MINOR PATCH <<< "$MAIN_VERSION"

        if [ "$PATCH_ONLY" = "true" ]; then
            PATCH=$((PATCH + 1))
        else
            MINOR=$((MINOR + 1))
            PATCH=0
        fi

        NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"

        sed "s/^version = \"${MAIN_VERSION}\"/version = \"${NEW_VERSION}\"/" pyproject.toml > pyproject.toml.tmp && mv pyproject.toml.tmp pyproject.toml
        git add pyproject.toml
        git commit --no-verify -m "chore: auto-bump version to ${NEW_VERSION}"

        echo ""
        echo -e "${GREEN}Auto-bumped version to ${NEW_VERSION}.${NC}"
        echo -e "${YELLOW}Push again to include the bump.${NC}"
        echo ""
        exit 1
    fi
fi

# =====================================================
# 2. PR Detection
# =====================================================

HAS_PR=false
IS_PR_INTENDED_BRANCH=false
SHOULD_BLOCK=false

if [[ "$CURRENT_BRANCH" == feature/* ]] || \
   [[ "$CURRENT_BRANCH" == fix/* ]] || \
   [[ "$CURRENT_BRANCH" == release/* ]]; then
    IS_PR_INTENDED_BRANCH=true
    SHOULD_BLOCK=true
fi

if command -v gh &> /dev/null; then
    if PR_INFO=$(gh pr view "$CURRENT_BRANCH" --json url,state 2>/dev/null); then
        PR_STATE=$(echo "$PR_INFO" | jq -r '.state' 2>/dev/null)
        if [ "$PR_STATE" = "OPEN" ]; then
            HAS_PR=true
            SHOULD_BLOCK=true
        fi
    fi
fi

echo ""
echo -e "${BLUE}======================================${NC}"
echo -e "${BLUE}  elspais Pre-Push Validation${NC}"
echo -e "${BLUE}======================================${NC}"
echo ""

if [ "$SHOULD_BLOCK" = true ]; then
    echo -e "${RED}Validation failures will BLOCK this push.${NC}"
else
    echo -e "${YELLOW}Branch '$CURRENT_BRANCH' has no open PR.${NC}"
    echo -e "${YELLOW}Validation failures will show warnings only.${NC}"
fi
echo ""

VALIDATION_FAILED=false
REPO_ROOT=$(git rev-parse --show-toplevel)

# =====================================================
# 3. E2E Tests (cached)
# =====================================================

CACHE_DIR="$REPO_ROOT/.results"
E2E_CACHE="$CACHE_DIR/.test-cache-e2e"
CONTENT_HASH=$(git write-tree 2>/dev/null | head -c 40)

mkdir -p "$CACHE_DIR"

# git holds the connection to the remote open for the whole of this hook, and
# the remote closes an idle connection well before a full e2e run finishes.
# When that happens git reports "Connection to <host> closed by remote host"
# and exits 141 -- a network-shaped error with nothing pointing back here. So
# the two guards below exist to keep this stage from running when it cannot
# tell us anything new, and the banner exists so the failure is diagnosable
# when it does run long.

# Guard 1: a push that changes only prose cannot change e2e behaviour. The
# documentation-sync stage below still covers the documents themselves.
E2E_RELEVANT=true
E2E_MERGE_BASE=""
if E2E_MERGE_BASE=$(git merge-base HEAD origin/main 2>/dev/null); then
    E2E_CHANGED=$(git diff --name-only "$E2E_MERGE_BASE" HEAD)
    if [ -n "$E2E_CHANGED" ] && ! printf '%s\n' "$E2E_CHANGED" | grep -qvE '(\.md$|^docs/)'; then
        E2E_RELEVANT=false
    fi
fi

# Guard 2: the cache records the verdict for a tree, not just its successes.
# Caching only passes meant any failing run -- including one caused by the
# local environment rather than the code -- guaranteed that every subsequent
# push re-ran the whole suite and died the same way.
E2E_CACHED_HASH=""
E2E_CACHED_VERDICT=""
E2E_CACHED_ENV=""
if [ -f "$E2E_CACHE" ]; then
    E2E_CACHED_HASH=$(cut -d' ' -f1 < "$E2E_CACHE")
    E2E_CACHED_VERDICT=$(cut -d' ' -f2 < "$E2E_CACHE")
    E2E_CACHED_ENV=$(cut -d' ' -f3 < "$E2E_CACHE")
fi

# Guard 3: the e2e tier spawns `elspais` as a subprocess from PATH, so its
# verdict describes whichever binary that resolved to -- not necessarily this
# tree. A stale global install (pipx, ~/.local/bin) or another worktree's venv
# produces failures that belong to the environment, and the tree hash cannot
# tell the two runs apart: same code, same key, different software.
#
# So identify the environment by where the on-PATH CLI's code actually lives,
# and make that part of the cache key. A verdict earned elsewhere is a miss,
# not a failure -- it simply has nothing to say about this tree, so we re-run.
# Recording it as a third field also retires every 2-field entry written
# before this guard existed: they have no environment, so they never match.
e2e_cli_src() {
    _bin=$(command -v elspais 2>/dev/null) || return 1
    _py=$(head -1 "$_bin" 2>/dev/null | sed 's|^#!||')
    [ -x "$_py" ] || return 1
    "$_py" -c 'import elspais,os;print(os.path.realpath(os.path.dirname(elspais.__file__)))' 2>/dev/null
}
# `set -e` is in force and `VAR=$(fn)` adopts the function's exit status, so an
# unset fallback aborts the whole hook when `elspais` is not installed — before
# the secret scan or the documentation check get to run. Not finding the CLI is
# a fact this stage already knows how to report; it is not fatal.
E2E_REPO_SRC=$(cd "$REPO_ROOT" && python3 -c 'import os,sys;print(os.path.realpath(sys.argv[1]))' "src/elspais") \
    || E2E_REPO_SRC="$REPO_ROOT/src/elspais"
E2E_ENV_SRC=$(e2e_cli_src) || E2E_ENV_SRC=""

# The venv in the worktree is the binary this tree's e2e tests mean. Prefer it
# automatically rather than making every developer remember the PATH dance.
if [ "$E2E_ENV_SRC" != "$E2E_REPO_SRC" ] && [ -x "$REPO_ROOT/.venv/bin/elspais" ]; then
    _saved_path="$PATH"
    PATH="$REPO_ROOT/.venv/bin:$PATH"
    export PATH
    if [ "$(e2e_cli_src)" = "$E2E_REPO_SRC" ]; then
        E2E_ENV_SRC="$E2E_REPO_SRC"
        echo -e "${YELLOW}E2E: using $REPO_ROOT/.venv/bin for elspais (PATH resolved elsewhere)${NC}"
    else
        PATH="$_saved_path"
        export PATH
    fi
fi

if [ "$E2E_ENV_SRC" != "$E2E_REPO_SRC" ]; then
    echo -e "${YELLOW}E2E: on-PATH elspais is ${E2E_ENV_SRC:-<not found>},${NC}"
    echo -e "${YELLOW}     not this tree ($E2E_REPO_SRC). Results describe that${NC}"
    echo -e "${YELLOW}     install, not this branch. Fix with:${NC}"
    echo -e "${YELLOW}       export PATH=\"$REPO_ROOT/.venv/bin:\$PATH\"${NC}"
fi

if [ -n "$ELSPAIS_E2E_FORCE" ]; then
    E2E_CACHED_HASH=""
    E2E_CACHED_VERDICT=""
    E2E_CACHED_ENV=""
fi

E2E_CACHE_USABLE=false
if [ "$E2E_CACHED_HASH" = "$CONTENT_HASH" ] && [ -n "$E2E_CACHED_ENV" ] \
   && [ "$E2E_CACHED_ENV" = "$E2E_ENV_SRC" ]; then
    E2E_CACHE_USABLE=true
fi

if [ "$E2E_RELEVANT" = false ]; then
    echo -e "${GREEN}E2E tests skipped — this branch changes documentation only${NC}"
elif [ "$E2E_CACHE_USABLE" = true ] && [ "$E2E_CACHED_VERDICT" = "PASS" ]; then
    echo -e "${GREEN}E2E tests cached — passed since last change${NC}"
elif [ "$E2E_CACHE_USABLE" = true ] && [ "$E2E_CACHED_VERDICT" = "FAIL" ]; then
    VALIDATION_FAILED=true
    echo -e "${RED}E2E tests failed (cached verdict for an unchanged tree)${NC}"
    echo "Re-run them with: ELSPAIS_E2E_FORCE=1 git push"
    echo "Cache: $E2E_CACHE"
else
    echo -e "${YELLOW}Running e2e tests. This stage takes several minutes, and git${NC}"
    echo -e "${YELLOW}holds the connection to the remote open while it runs. If the${NC}"
    echo -e "${YELLOW}push ends in \"Connection ... closed by remote host\", that is${NC}"
    echo -e "${YELLOW}this stage overrunning the remote's idle timeout, not a network${NC}"
    echo -e "${YELLOW}fault: the verdict is cached, so pushing again completes quickly.${NC}"
    echo ""

    E2E_RC=0
    pytest tests/ -m "e2e" --tb=short -q \
        --junitxml="$CACHE_DIR/test-results-e2e.xml" 2>&1 || E2E_RC=$?

    if [ "$E2E_RC" -ne 0 ]; then
        VALIDATION_FAILED=true
        echo "$CONTENT_HASH FAIL $E2E_ENV_SRC" > "$E2E_CACHE"
        echo -e "${RED}E2E tests failed!${NC}"
    else
        echo "$CONTENT_HASH PASS $E2E_ENV_SRC" > "$E2E_CACHE"
        echo -e "${GREEN}E2E tests passed!${NC}"
    fi
fi
echo ""

# Pretty-print JUnit XML for elspais line-level linking
for xml_file in "$CACHE_DIR"/test-results*.xml; do
    [ -f "$xml_file" ] || continue
    python3 -c "
import xml.etree.ElementTree as ET, sys
try:
    tree = ET.parse('$xml_file')
    ET.indent(tree, space='  ')
    tree.write('$xml_file', encoding='unicode', xml_declaration=True)
except Exception:
    sys.exit(0)
" 2>/dev/null || true
done

# =====================================================
# 4. Secret Detection
# =====================================================

echo -e "${BLUE}Running secret detection...${NC}"
echo ""

if command -v gitleaks &> /dev/null; then
    REMOTE_BRANCH=$(git rev-parse --abbrev-ref --symbolic-full-name @{u} 2>/dev/null || echo "")
    if [ -n "$REMOTE_BRANCH" ]; then
        if ! gitleaks detect --source="$REPO_ROOT" --log-opts="$REMOTE_BRANCH..HEAD" --no-banner --redact 2>&1; then
            VALIDATION_FAILED=true
            echo -e "${RED}Gitleaks detected secrets!${NC}"
        else
            echo -e "${GREEN}No secrets detected!${NC}"
        fi
    else
        if ! gitleaks detect --source="$REPO_ROOT" --no-banner --redact 2>&1; then
            VALIDATION_FAILED=true
            echo -e "${RED}Gitleaks detected secrets!${NC}"
        else
            echo -e "${GREEN}No secrets detected!${NC}"
        fi
    fi
else
    echo -e "${YELLOW}WARNING: gitleaks not installed - skipping${NC}"
fi
echo ""

# =====================================================
# 5. Documentation Sync Tests
# =====================================================

echo -e "${BLUE}Running documentation sync tests...${NC}"
echo ""

if ! pytest tests/test_doc_sync.py -v --tb=short 2>&1; then
    VALIDATION_FAILED=true
    echo -e "${RED}Documentation sync tests failed!${NC}"
else
    echo -e "${GREEN}Documentation sync tests passed!${NC}"
fi
echo ""

# =====================================================
# 6. Results
# =====================================================

echo -e "${BLUE}======================================${NC}"
echo -e "${BLUE}  Validation Summary${NC}"
echo -e "${BLUE}======================================${NC}"
echo ""

if [ "$VALIDATION_FAILED" = true ]; then
    if [ "$SHOULD_BLOCK" = true ]; then
        echo -e "${RED}VALIDATION FAILED — push BLOCKED${NC}"
        echo ""
        echo "To bypass (NOT RECOMMENDED): git push --no-verify"
        exit 1
    else
        echo -e "${YELLOW}VALIDATION FAILED (warnings only — no open PR)${NC}"
        exit 0
    fi
else
    echo -e "${GREEN}ALL VALIDATIONS PASSED${NC}"
    exit 0
fi
